# Backlog — sailing-architects

> **Jedyne źródło prawdy dla OTWARTYCH pozycji.** Ostatni reconcile: 2026-08-13 (II).
>
> Zasady:
> - Tu trafia **każda** otwarta pozycja (bug / feature / infra). Rozwiązane → skreśl `~~…~~` z datą albo usuń.
> - Szczegóły dużych list żyją w dedykowanych plikach (poniżej linkowane) — tu tylko 1-linijkowy wpis + wskaźnik.
> - `handoff.md` = narracja sesji (co się stało). **Nie jest** backlogiem.
> - Close-session: przenieś nowe otwarte tu, oznacz rozwiązane. Jeden przebieg = lista uczciwa.
> - ID stabilne — nie zmieniaj po nadaniu.

---

## 🔴 Bugi otwarte

- ~~**BUG-1 — Panel żeglarza: „Cała trasa rejsu" zawsze podświetla Gibraltar→Madera.**~~ ✅ 2026-07-13 (hardcoded `active` usunięte z `ports[]`; `activeLeg` derived ze `slug` segmentu bookingu, mapa s1..s4→0..3; reaguje na selektor multi-booking). *(dup: admin-post-mvp „…zawsze podświetla Gibraltar → Madera" — skreślić i tam)*
- ~~**BUG-2 — Checkout krok 4 (`/book?segment=s1`): przycisk „Wróć" nie działa.**~~ ✅ 2026-07-13 (commit `15261fa4`: ping-pong back()↔$effect; łańcuch wstecz zalogowanego 4→3→1). *(feedback 07-05; ta sama uwaga w docx Michała 06-19)*
- ~~**BUG-3 — Po kliknięciu „Rezerwuj" klik na „Panel" nie działa** („Poradnik" działa)~~ ✅ 2026-07-13 (nie overlay: same-route nav `/book`→`/book?auth=` nie remountuje komponentu, `initialAuthParam` czytany raz w init; fix: `$effect` na reaktywnym `authParam` — zalogowany → `panelTarget()`, wylogowany → step 2). *(feedback 07-05)*
- ~~**BUG-4 — Alert „Held kończy się za X min" zamrożony.**~~ ✅ 2026-07-15 (commit `650887e9`: query zwraca surowe `holdExpiresAt?` w alercie, klient formatuje z reactive clock `{@const}` + ternary; na prod). *(dup: admin-post-mvp „Reactive clock dla odliczania held" — skreślić i tam)*
- ~~**BUG-5 — `/book` Step 5: fallback „Całość" maskuje brak planu w bazie.**~~ ✅ 2026-07-16 (commit `cc28966`: fallback → `[]`, template z gałęziami loading / query-error / brak-planu / radia; smoke test przez `isActive: false` w Dashboardzie OK). *(dup: admin-post-mvp „`/book` Step 5 — fallback…" — skreślić i tam)*
- ~~**BUG-6 — Admin: brak akcji „Wyloguj" w `/admin` w ogóle** + brak avatar/user-menu.~~ ✅ 2026-07-15 (commit `1274523d`: `<UserButton />` w admin sidebar dół + mobilebar + dashboard header; dark theme popovera naprawiony — nowe nazwy zmiennych Clerk + `elements.userButtonPopoverActionButton`; na prod). Style `.btn--signout` zostają — reużyte przez selektor rejsu. *(dup: admin-post-mvp „Brak akcji Wyloguj w layoucie /admin" — skreślić i tam)*
- ~~**BUG-7 — Walidacja formy edycji uczestnika w drawerze.**~~ ✅ 2026-07-26 — kod w `f37903b0` (`adminParticipantSchema` format-only w `crew-profile.ts`; walidacja klient w drawerze z `novalidate` + serwer w mutacji z `throw`; przy okazji drawer `type="date"` ISO → tekstowe `dd/mm/yyyy` jak reszta app). **Smoke test warstwy serwerowej zaliczony 07-26:** obejście klienta (guard zakomentowany + `...parsed.data` → `...payload`) → email `abc` → `mutations.ts:868` rzucił „Podaj poprawny adres e-mail"; po przywróceniu diff pusty, toast błędu bez wywołania sieciowego. Sam guard zakomentowany nie wystarcza — `parsed.data` jest wtedy `undefined`, spread to no-op i mutacja dostaje pusty patch (objaw: toast sukcesu + nic się nie zapisuje). Ogon: **INFRA-7**. *(admin-post-mvp „Walidacja pól formy…" — skreślić i tam)*

- ~~**BUG-8 — Monity o zaległe raty wychodzą do rezerwacji po zwrocie.**~~ ✅ 2026-07-26 (commit `93631829`, na prod). Trzy maile „Zaległa rata…" wyszły z produkcji o 09:10 UTC do zwróconych rezerwacji testowych (SA-2026-9562, SA-2026-1708, SA-2026-2443; odbiorca = własna skrzynka, żaden klient nie zobaczył). Przyczyna: `_listUpcomingPaymentCandidates` i `_listOverduePaymentCandidates` filtrowały tylko `booking.status !== 'confirmed'`, a zwrot **nie zmienia statusu**. Fix: ten sam blok `isBookingClosed` + `continue`, który cron danych załogi dostał 07-24, wstawiony za tanimi filtrami i przed `resolveBuyerRecipient`. Czwarta odsłona wzorca „fakt wyliczany nie broni się sam" (KPI 07-04, panel żeglarza 07-07, cron załogi 07-24). Ogony z przeglądu: **REFACTOR-5**, **SEC-4**.
- ~~**BUG-9 — Późny webhook płatności może nadpisać `paymentStatus: 'refunded'`.**~~ ✅ **2026-08-13** (`d0f841cf`, na prod). Guard w `applyStripePayment` ([`mutations.ts:590`](../src/convex/mutations.ts)) — wcześniejsze wyjście, gdy `booking.paymentStatus === 'refunded'`; w webhooku `if (!result) break` przed pierwszym użyciem wyniku. Wybrany kierunek to **guard**, nie „`refreshBookingPaymentTotals` świadoma zwrotów" — ten drugi zostaje jako naprawa u źródła, gdy pojawi się drugi konsument. `partially_refunded` **świadomie poza warunkiem** (ADR-020: częściowy zwrot to korekta ceny, rezerwacja trwa, kolejne raty muszą się księgować). Odmowa zwraca Stripe'owi `200`, nie `throw` — inaczej dostawca ponawiałby trzy dni, a lista nieudanych zdarzeń przestałaby znaczyć „awaria". **Dwa ogony:** (1) **FEAT-18** — Michał nadal nie dostaje żadnego sygnału o wpłacie po zwrocie (decyzja biznesowa spisana w **ADR-022**); (2) **smoke test niewykonany** — odłożony do FEAT-18 decyzją Tomka, więc poprawka jest na produkcji zweryfikowana wyłącznie typecheckiem i buildem. Przepis testu, nie odtwarzać od zera: kup 5 zł na live → sprawdź `paid` (**to jest próba kontrolna** — wychwytuje odwrócony warunek, który blokowałby wszystkie płatności) → pełny zwrot → sprawdź `refunded` → w dashboardzie Stripe **Resend** zdarzenia `payment_intent.succeeded` z kroku 1 → teza: status dalej `refunded`.
- ~~**BUG-9 (opis pierwotny, 08-08)**~~ — Scenariusz: rata w stanie `processing` w momencie pełnego zwrotu (ADR-020, uzupełnienie 08-08) zostaje świadomie nietknięta — poprawnie, bo o niej rozstrzyga Stripe, nie baza. Ale gdy webhook `payment_intent.succeeded` dla tej raty dojdzie **po** zwrocie, [`applyStripePayment`](../src/convex/mutations.ts) bezwarunkowo wywołuje [`refreshBookingPaymentTotals`](../src/convex/mutations.ts) (linia z `const totals = await refreshBookingPaymentTotals(ctx, booking)`), która liczy `paymentStatus` funkcją `bookingPaymentStatus` — zwraca wyłącznie `unpaid | deposit_paid | partially_paid | paid`, nie zna pojęcia zwrotu — i **nadpisuje `refunded` ustawione wcześniej przez `processStripeRefund`**. Booking wygląda po tym jak w pełni opłacony, mimo że pieniądze oddano. Webhook payment_intent.succeeded (`src/routes/api/stripe/webhook/+server.ts`) nie ma żadnego guardu zamkniętej/zwróconej rezerwacji przed wywołaniem mutacji — ten sam kształt braku co SEC-4. Prawdopodobieństwo niskie (wymaga zbiegu w czasie webhooka sukcesu i zwrotu dla tej samej raty), ale skutek to cichy, błędny stan finansowy bez żadnego alertu. Kierunek naprawy do rozstrzygnięcia: guard w `applyStripePayment` (nie nadpisuj statusu booking, jeśli już `refunded`/`partially_refunded`) albo `refreshBookingPaymentTotals` świadoma zwrotów. Trzecia opcja zaproponowana przez Tomka przy okazji tej rozmowy (08-08): zamiast czysto technicznego guardu, informować Michała że rata jest w toku i domykać sprawę webhookiem/powiadomieniem po rozstrzygnięciu — realny kierunek, ale osobny feature (kanał powiadomień + stan pośredni w UI), nie jednolinijkowa poprawka. *(wykryte 08-08 przy teście ADR-020, Claude — nie zgłoszone przez Tomka; kierunek naprawy #3 — pomysł Tomka)*
- **BUG-10 — Panel klienta: rata po terminie nadal pokazuje „Oczekuje" zamiast „Po terminie".** Michał, feedback `docs/feedback/2026-08-12.md` (screenshot `Screenshot 2026-08-12 at 17.26.22.png`): w „Harmonogramie płatności" prosi o napis „Zaległe", gdy termin minął. `paymentRowStatusLabel` (`dashboard/+page.svelte:104-119`) czyta wyłącznie zapisany `payment.status` — etykieta „Po terminie" pojawia się dopiero gdy status w bazie realnie stanie się `'overdue'`, co robi osobny cron (`markOverduePayments`), nie porównanie z zegarem w przeglądarce. Między minięciem `dueAt` a najbliższym przebiegiem crona wiersz pokazuje „Oczekuje", mimo że termin już minął. Komponent i tak trzyma żywy `now` (interval co sekundę, `onMount:125-134`, dziś używany gdzieś indziej) — nieużyty tu. Ta sama rodzina co „Storage vs derive — KPI vs drawer" z sesji 2026-05-08 (KPI liczy `overdue` z `dueAt < now`, drawer czyta surowy status — dwa źródła prawdy). Fix: `paymentRowStatusLabel` dostaje też `dueAt`, etykieta „po terminie" derywowana z `dueAt < now` dla statusów `pending`/`failed`, nie tylko ze stringa `overdue`. **Otwarte: czy tekst etykiety zmienić na „Zaległe" (słowo Michała) czy zostawić istniejące „Po terminie"** — nie ten sam wybór co UI-17 (tam decyzja prawna), tu czysto stylistyczne, do potwierdzenia. *(Michał, feedback 08-12; striaż Claude 08-13)*

## 🚀 Deploy

- ~~**DEP-1 — Deploy zbiorczy:** snapshot polityki + A7e cron + audit UI + admin nav shell.~~ **✅ ZDEPLOYOWANE 2026-07-10** (`push origin main:production` → `efb399fc..f289608b`; CRON_SECRET w Vercel; build zielony; Vercel Cron zarejestrowany). Zostaje **walidacja na prod** (nie sam deploy):
  - **DEP-1a — Test odporności snapshotu na prod** (właściwy dowód prawny): kup rejs → snapshot zamraża % → admin zmienia próg → zwrot → sugestia trzyma snapshot, nie żywą. **⏸ Odłożone 2026-07-30 — nie ma jeszcze realnych wpłat na produkcji, więc nie ma czego zwracać. Wrócić nie wcześniej niż 2026-08-30** (albo wcześniej, gdy pojawi się pierwsza realna wpłata). **Projekt testu gotowy, nie odtwarzać go od zera:** (1) zapisz stan przed — aktualne progi z `/admin/automation` + sugestię (`%`, kwota, dni przed wypłynięciem) dla rezerwacji **ze** snapshotem **i** dla rezerwacji **bez** snapshotu (sprzed 07-10, idzie fallbackiem na żywą politykę); (2) zmień próg; (3) rezerwacja ze snapshotem — sugestia **bez zmian** (teza), rezerwacja bez snapshotu — sugestia **zmieniona** (próba kontrolna); (4) przywróć progi. **Krok 3 bez kontroli nic nie dowodzi** — „bez zmian" wypada tak samo, gdy zapis progu w ogóle nie doszedł (ta sama luka co KPI Held 07-26 i pusty wynik crona 07-24). Mechanizm w kodzie: `refunds.ts:39-50`, snapshot wygrywa, żywa polityka to fallback. Powiązane: [[unchanged-result-needs-a-control]].
  - **DEP-1b — A7e na realnym stuck refund:** wymuś pending bez webhooka → cron/curl z `CRON_SECRET` → koja wraca + mail.
  - **Uwaga 2026-08-13:** warunek „pierwsza realna wpłata" spełniony — Stripe przełączony na live konto („Skipper Arr"), test 5 zł kupno+zwrot przeszedł end-to-end (`paid` → `refunded`). To **nie jest** jeszcze test DEP-1a (nie zmieniano progu w trakcie), ale odblokowuje jego wykonanie, gdy będzie na to czas.

## ✨ Features (większe)

- **FEAT-1 — PDF itinerary rejsu do pobrania,** gated za auth (tylko uczestnicy). INNY niż PDF potwierdzenia. *(Michał 06-19 #18)*
- **FEAT-2 — Faktury + KSeF** (duży moduł, research 2026-07-03). Convex action, FA(3) XML, cert KSeF typ 1 Michała. Szczegóły: handoff „Otwarte problemy".
- **FEAT-3 — Audit log rozbudowa:** filtry server-side (`by_action`/`by_booking`) + `policy_updated` before→after diff. MVP już żyje (`/admin/audit`).
- **FEAT-4 — unhandledStripeEvents resolution UI** (refundy z Stripe Dashboard) — lista + decyzje admina (release/keep/orphan), `resolution` enum już w schema.
- **FEAT-5 — Multi-rejs / multi-segment platform + teaser przyszłych rejsów.** Przerobić landing + bazę pod wiele rejsów, tak by współistniały: aktualny (wyprzedany lub w sprzedaży) + zapowiadany nowy. Elementy: (a) zakładka np. „REJS GRECJA maj 2027" z opisem + „termin wkrótce"; (b) **wysuwana belka pod menu** „chcę szczegóły" → email capture → powiadomienie przy starcie sprzedaży + **5% rabat** early-bird; (c) **rozbudowa menu — przechodzenie między rejsami**. DUŻY, przemyśleć cały landing. *(Michał 07-07 #3 + Tomek 07-10 #1-3)* **Pierwszy konkretny drugi rejs (Michał 07-12): Seszele, katamaran Lagoon 40, 4 kajuty×2 os. (prywatne łazienki, klima, agregat, odsalarka), maj 2027, 2 terminy: 8-18 i 18-29 maja (można 22 dni), 1150 EUR/os. + przelot + pokładówka. Plakat: `docs/assets/WhatsApp Image 2026-07-10 at 15.34.38.jpeg` — termin na plakacie BŁĘDNY.** **Rozszerzenie (Michał email 07-23 #3 + Tomek 07-24):** strona główna z wyborem kilku rejsów (obecna zostaje jako strona pojedynczego rejsu); najbliższe do dodania: Seszele maj, Francja wina kwiecień, Grecja czerwiec. Do tego: (d) **mechanizm zakładania nowych rejsów przez Michała w `/admin`** (self-service, spójne z FEAT-11); (e) rozważyć **Cloudinary jako zewnętrzne źródło grafik** (wzorzec znany z projektu `domy-modulowe`).

  **Do ADR przy projektowaniu (Tomek 07-31 (II), wyszło z INFRA-9 — zapisać teraz, dopóki znane są powody):** modelować **dwie encje, nie jedną**. Dziś są zrośnięte, bo jacht jest jeden i pływa jedną trasą, więc wszystko wygląda na jeden byt. Test rozdzielający: *co musiałoby się zmienić, żeby ta wartość się zmieniła?* — `~2200nm` i daty → **trasa (REJS)**; liczba koi, plan kabin, specyfikacja → **jacht (JACHT)**. Ta sama linia przechodzi przez pliki danych: `cabins.ts` opisuje jacht, `voyage-segments.ts` opisuje wyprawę, a pasek `stats` w hero miesza oba (`10 miejsc` z jachtu, `41 dni` z wyprawy) i nikomu to dziś nie przeszkadza. **Seszele rozerwą ten szew** — inny akwen, inny jacht (Lagoon 40, 4 kajuty × 2 = 8 koi wobec 10 na Sun Odyssey). Jeśli drugi rejs wejdzie jako „kolejny zestaw segmentów", plan koi pojedzie z Atlantyku, a objaw wypłynie jako błąd walidacji przy wyborze nieistniejącej koi — **daleko od miejsca decyzji**. Osobno: wartości wyliczalne (liczba etapów, łączna długość) **nie są polami** żadnej z encji. Pozycje mieszane („Pełne ubezpieczenie jachtu i kaucji" zależy i od jachtu, i od organizatora) oddać wprost Michałowi zamiast przypisywać na wyczucie — patrz INFRA-10. Powiązane: [[one-instance-hides-two-entities]], REFACTOR-6, REFACTOR-2, FEAT-11.
- **FEAT-6 — Indywidualne ceny koi** (rabat per koja; boczna kajuta taniej „gdy chcemy"). Mechanizm: procent rabatu przy koi albo opis + reużycie działającego mechanizmu zwrotu. *(Michał 07-07 #2 + Tomek 07-07 #2)*
- **FEAT-7 — Płeć żeglarza** w schemacie + opcjonalne pokazywanie K/M na sprzedanych miejscach. *(Michał 07-07 #4)*
- ~~**FEAT-8 — Badge „BRAK MIEJSC"** gdy odcinek/rejs wyprzedany + licznik „Wolne N miejsc" gdy mało.~~ ✅ 2026-07-25 (commit `d8f9b246`, na prod, zweryfikowane): query `listBerthAvailability` (bez argumentów, `{ slug, free }[]` dla wszystkich etapów), reguła holdu w `src/convex/_lib/berthFree.ts`, próg + teksty w `src/lib/berth-badge.ts` (`LOW_STOCK = 3`), badge w `cabins-section` (zakładki) i `pricing-section` (karty cennika), warianty `--low-stock` (mosiądz) / `--sold-out` (neutralny, wyższy kontrast). **Przy okazji usunięty zaszyty badge „Ostatnie miejsca" na czwartej karcie cennika** — wisiał na etapie z największą liczbą wolnych koi. Badge nie blokuje przycisku (wyprzedany etap nadal klikalny). *(Michał 07-07 #1 + Tomek 07-24)*
- **FEAT-9 — Miejsca specjalne: podpiąć edycję danych żeglarza** (`/admin/special`). *(Tomek 07-07 #1)*
- **FEAT-11 — Admin: samodzielna edycja cen segmentów i parametrów landingu (Michał).** Feedback Michała 07-17: nie mógł zmienić ceny całkowitej rejsu („pole ceny nieaktywne") ani długości trasy (km→nm). Stan dzisiejszy: cena żyje w **4 miejscach** — (1) Convex `voyageSegments.pricePerBerth` (jedyne źródło naliczania w Stripe `create-intent`!), (2) `src/lib/data/voyage-segments.ts` (UI checkout), (3) `route-section.svelte` (landing, hardcoded lista), (4) kwoty rat w planach płatności (`/admin/automation`). Zmiana ceny = ręczna synchronizacja wszystkich 4 (Tomek 07-17: „centy musiałem zmieniać w trzech miejscach" + patch DB). Docelowo: jedno źródło (DB) + UI admina do edycji ceny segmentu i statów landingu (długość trasy w nm). Uwaga na snapshot: istniejące bookingi trzymają kopie — zmiana ceny działa tylko w przód. *(Michał 07-17 + Tomek 07-17; Michał ponowił 07-19: „pole cena aktywne, żebym sam mógł zmieniać ceny w dowolnym momencie")*
- **FEAT-12 — Sprzedaż książki-poradnika online + kod rabatowy 3% + darmowy śpiewnik.** Michał kończy książkę dla nowicjuszy; sprzedaż online w sekcji „Poradnik"; każdy zakup generuje jednorazowy kod −3% do wykorzystania przy rezerwacji. Obok: darmowy śpiewnik żeglarski do pobrania (pdf, mobi, epub). Wymaga decyzji: Stripe product/checkout dla książki, generowanie i walidacja kodów, dostawa (e-book? papier?). *(Michał email 07-23 #4)*
- **FEAT-13 — Lightbox na zdjęciach (jacht + przyszła galeria „Program rejsu").** Zdjęcia w sekcji jacht i w galerii programu rejsu powiększają się po kliknięciu do formy galerii. *(Michał email 07-23 #1-2)* **Zrobione 07-24** (natywny `<dialog>` w `vessel-section.svelte`: klik hero/miniatura → showModal, prev/next z wrap-around, Esc/×/strzałki klawiatury, backdrop). **Znane ograniczenie: źródła w `static/images/sailing/` mają niską rozdzielczość (max 698×548px)** → na Full HD lightbox nie może powiększyć bez rozmycia; sufit ustawiony na `min(92vw, 1000px)` (łagodny upscale ~1.4×). Prawdziwy fix = zdjęcia w pełnej rozdzielczości od Michała, wchodzą z FEAT-5e (Cloudinary) / FEAT-15 (galeria „Program rejsu"). *(Tomek 07-24)*
- **FEAT-14 — Dział/blog „po rejsach" — wgrywanie materiałów (deadline: październik 2026).** Nowy cały dział na relacje/materiały po odbytych rejsach (zdjęcia, opisy). Prawdopodobnie razem z Cloudinary (FEAT-5e). *(Tomek 07-24)*
- **FEAT-15 — Sekcja „Program rejsu" nad sekcją jacht.** Czeka na materiały: opis rejsu + zdjęcia od Michała. Galeria zdjęć z lightboxem (FEAT-13). *(Michał email 07-23 #1; BLOCKED na content)*
- **FEAT-16 — RABAT jako encja: dziś nie istnieje, a jest potrzebny w czterech miejscach.** Obniżenie ceny pojawia się w backlogu trzy razy jako doklejka do innej funkcji — **FEAT-5 (b)** 5% early-bird za zapis na powiadomienie, **FEAT-6** rabat per koja („boczna kajuta taniej"), **FEAT-12** jednorazowy kod −3% za zakup książki. Czwarty przypadek wyszedł 08-06: **korekta ceny dla konkretnego klienta**, którą Michał robi dziś **zwrotem**, bo nie ma czym innym — a zwrot technicznie dotyczy wyłącznie rat już opłaconych i nie zmienia kwoty rat przyszłych (patrz ADR-020, punkt 4). Cztery funkcje, każda wymyśli własny mechanizm obniżania ceny, jeśli nic ich nie spina. Kierunek: rabat jako byt z kwotą/procentem, powodem i (czasem) kodem, przypinany do rezerwacji albo do pozycji planu płatności — do rozstrzygnięcia, czy nośnikiem są kupony Stripe, czy własna tabela. Wchodzi **przed** którąkolwiek z FEAT-5b / FEAT-6 / FEAT-12, inaczej pierwsza z nich ustawi kształt dla pozostałych. Ta sama rodzina co REFACTOR-6 p.5 (port bez encji): ta sama rzecz pod kilkoma nazwami. *(Tomek 08-06, przy REFACTOR-5)*
- **FEAT-10 — Checkout: przemyśleć miejsce kroku 3 (dane załogi).** Obserwacja przy BUG-2: krok 3 miesza dane żeglarza z procesem płatności; jest hybrydą etapu flow i formularza-detour z kroku 4 (stąd niejasny przycisk „Zapisz i wróć"). Dane są już opcjonalne i uzupełnialne po płatności w panelu → kandydat: wyjąć krok 3 z checkoutu (zostawić tylko ścieżkę panelową + detour z podsumowania) albo przesunąć za płatność. Decyzja produktowa — zdanie Michała. *(Tomek 07-13, przy naprawie BUG-2)*
  **Potwierdzenie 2026-08-13, przy teście Stripe live cutover:** krok 4 (`book/+page.svelte:1091-1101`) ma box „Dane żeglarzy możesz uzupełnić po płatności" + przycisk „Uzupełnij teraz", wciśnięty między podsumowanie a „Przejdź do płatności →". Tomek: myli, bo sugeruje wybór teraz/później dokładnie przed krokiem pieniędzy. Jego propozycja: przenieść ten box poza sekwencję potwierdzenie→płatność (na koniec, po płatności), tak żeby krok płatności szedł czysto, bez rozgałęzienia. Ten sam kierunek co pierwotny opis FEAT-10, konkretny fragment UI do ruszenia gdy przyjdzie decyzja Michała.
- **FEAT-17 — Automatyzacja zgłoszenia załogi (booking-manager.com).** Michał 08-10: dostaje per-rejs link do formularza booking-manager od firmy czarterowej (min. 10 dni przed rejsem), musi go wypełnić i wysłać do dnia rozpoczęcia rejsu; potrzebuje zamkniętej listy 5 dni przed rejsem (do wydruku, musi mieć papier przy sobie). Propozycja Michała: appka zbiera dane załogi (już to robi — `bookingParticipants`), dostaje pole na wklejenie linku booking-manager, **5 dni przed rejsem automatycznie wypełnia i wysyła formularz** (warunkowane kompletem danych), po wysyłce pobiera PDF i **mailuje go do Michała** do wydruku. **Otwarte pytanie techniczne, rozstrzygnąć przed jakąkolwiek wyceną:** czy booking-manager.com ma API, czy to dosłowne wypełnianie cudzego formularza w przeglądarce (automatyzacja na stronie trzeciej — inna klasa ryzyka: regulamin serwisu, kruchość przy zmianie ich UI, brak kontroli nad błędami). Bez odpowiedzi rozmiar zadania jest nieznany. *(Michał 08-10, striaż Claude 08-13)*

- **FEAT-18 — Rozbieżność finansowa wymagająca decyzji człowieka: brakująca encja + powiadomienie.** Wynika z **ADR-022** punkt 3, niezaimplementowanego. Dziś wpłata przychodząca do rezerwacji `refunded` jest **ignorowana po cichu** — dane są poprawne (BUG-9 naprawiony), ale pieniądze leżą u Stripe'a i nikt nie dostaje sygnału. **Sprawdzone i odrzucone jako miejsce zapisu, nie powtarzać:** (a) `adminAuditLog` — `adminUserId` jest wymagane, a aktora nie ma (zdarzenie przysłał Stripe), plus `action` to zamknięta lista czynności klikanych przez człowieka; (b) `unhandledStripeEvents` — dotyczy zdarzeń **niedopasowanych** do rezerwacji (`resolution`: `release_berth` / `keep_berth` / `orphan` = ustalanie przynależności), a tu zdarzenie zostało dopasowane i **świadomie odrzucone**. Dwa kandydaty odpadające z dwóch rozłącznych powodów = brakująca encja (rozpoznanie Tomka). **Tańszy kierunek do rozważenia przed nową tabelą:** rozbieżność już istnieje w danych i da się ją **wykryć skanem** — rata ma `status: 'cancelled'`, a Stripe dla tego samego `stripePaymentIntentId` raportuje `succeeded`; infrastruktura crona istnieje (A7e reconciliation, `/api/admin/refunds/reconcile`). Wyświetlenie: Alert Queue w `/admin` ma już wzorzec per-segment z `kind: 'refund_stuck'` (A7c krok 6). **Odblokowuje smoke test BUG-9.** *(Tomek 08-13, przy naprawie BUG-9)*

## ⚖️ Legal / compliance

- **LEGAL-1 — Strona RODO pod `/rodo`** (`www.sailing-architect.com/rodo`). Wymagane prawnie. *(Michał 07-07 #6)*
- **LEGAL-2 — Polityka prywatności** + link wklejony do regulaminu. Wzór: https://domy-modulowe.eu/polityka-prywatnosci *(Michał 07-07 #7)*
- **LEGAL-3 — Słowo „zaliczka" → „Wpłata początkowa".** ✅ część główna 07-24 (commit `aabe018`): copy w kodzie (labels, maile, PDF, crew-guide, automation, dashboard, seed) + label aktywnego planu na prod przez `/admin/automation`. **Zostaje:** (a) potwierdzenie nazwy z Michałem (proponował też „1 rata" — kolizja z numeracją istniejących „Rata 1/2"); (b) decyzja: czy historyczne snapshoty `bookingPayments.label` z „Zaliczka" też czyścić (celowo nietknięte — historia), pytanie prawne. *(Michał 07-17 dopisek, zrobione 07-24)*
- **LEGAL-4 — Podstawa prawna, miejsce i retencja danych wrażliwych (dowód/paszport) + transfery do USA.** Michał prosi o przejrzenie, jak radzi sobie z tym duży przewoźnik (wskazał regulamin RyanAira jako wzór): na jakie przepisy UE i porozumienia UE-USA się powołują, gdzie deklarują trzymanie danych, jak opisują retencję. Do rozstrzygnięcia także jego pytanie: co zrobić, gdy **podmiot trzeci użyty w technologii** (Stripe, Clerk, Brevo) sam żąda danych wrażliwych od żeglarza — czyj to wtedy proces i co musi być w polityce. Warstwa techniczna leży w SEC-1 (szyfrowanie + minimalizacja) i SEC-2b (DPA, hosting USA); ta pozycja jest o **treści prawnej**, którą trzeba napisać w LEGAL-1/LEGAL-2, nie o kodzie. *(Michał 07-20 dopisek #2 i #4)*
- ~~**INFRA-9 — Audyt zaślepek i danych makietowych w całym projekcie.**~~ ✅ 2026-07-31 (audyt wykonany; dwie zaślepki naprawione w jednym commicie, reszta rozpisana na pozycje). **Metoda:** trzy sygnatury, każda innym narzędziem. (b) `disabled` bez warunku — `rg --pcre2 'disabled(?![=:])'`, 26 trafień, 23 to CSS (`:disabled` jako pseudoklasa — szum wszedł stroną, której lookahead nie pilnował), 3 w markupie. (a) literały danych — pierwszy wzorzec `const \w+ = \[` **pominął `voyage-segments.ts` i `cabins.ts`**, bo oba mają `export const` i adnotację typu przed `=`; poprawny: `^\s*(export\s+)?const \w+[^=]*= \[`. (c) copy obiecujące funkcję — **niewykrywalna wzorcem**, wyszła z lektury widoku. **Wyniki:** 2 zaślepki naprawione dziś (kontakt z organizatorem + obietnica „do 30 dni przed rejsem"), 2 duplikaty danych → **REFACTOR-6**, 1 mylna etykieta → **UI-15**, treść landingu → **INFRA-10**, konwencja zapobiegawcza → **INFRA-8**. **Sprawdzone jako poprawne:** `dashboard:483` i `book:1288` (gołe `disabled` w gałęzi `{:else}` = stan, nie stała), `route-section:60 ports` (jedyne źródło współrzędnych, nie duplikat), słowniki UI (`DOC_TYPE_OPTIONS` i pokrewne — powielone w dwóch plikach, ale to inna rodzina niż dane rejsu). Powiązane: [[disabled-placeholder-still-asserts]], [[absent-rule-cannot-be-grepped]], [[empty-search-result-needs-a-control]], [[code-search-pattern-syntax-blind-spots]]. *(pierwotny opis:)* LEGAL-6 p.3 pokazał, że makieta potrafi pojechać na produkcję i wisieć tam niezauważona, bo wygląda jak gotowy widok. Zakładka „Dokumenty" wymieniała cztery pliki, z których istniał jeden — nikt tego nie zgłosił przez miesiące. Przejść cały projekt i wypisać wszystko, co udaje dane. **Czego szukać:** (a) literały tablicowe z polami wyglądającymi na produkcyjne (daty, kwoty, nazwy plików, numery); (b) `disabled` wpisane na stałe, bez warunku — zwykle znaczy „jeszcze nie zrobione"; (c) copy obiecujące funkcję, której nie ma. **Trafienia z sondażu 07-31, do potwierdzenia:** (1) `dashboard/+page.svelte:487` — przycisk „Kontakt z organizatorem" jest `disabled` bezwarunkowo, czyli nigdy nie działa (kontakt do Michała istnieje w innych miejscach aplikacji, więc to luka do zamknięcia, nie do usunięcia); (2) `dashboard/+page.svelte:~212` — tablica `ports` z datami etapów wpisana ręcznie, duplikat `voyageSegments`; daty wyglądają na prawdziwe, ale to drugie źródło prawdy utrzymywane ręcznie (rodzina REFACTOR-2 i FEAT-11). Sprawdzone jako **poprawne**, nie wymagają zmian: `dashboard:483` i `book:1288` — `disabled` w gałęzi `{:else}`, czyli warunkowe. Kryterium oceny dla każdego znaleziska: **czy sam tekst byłby prawdziwy, gdyby przycisk działał?** Powiązane: [[disabled-placeholder-still-asserts]]. *(Tomek 07-31, po LEGAL-6 p.3)*
- **LEGAL-6 — Regulamin rejsu nie istnieje jako dokument dostępny dla żeglarza.** Treść **jest** — Michał dosłał ją 07-07, plik `docs/business-decisions/2026_07_07_SA_regulamin_rejsu.doc` (przeniesiony tam 07-30 z `docs/feedback/`, bo to źródło prawne progów zwrotów w ADR-002 §3.8, nie przemijający feedback). Poza tym plikiem regulaminu nie ma nigdzie: brak trasy `/regulamin`, brak pliku w `static/`, brak odnośnika w stopce. **A panel twierdzi, że jest:** `dashboard/+page.svelte:221` renderuje na sztywno pozycję `{ name: 'Regulamin rejsu', date: '01.01.2026', type: 'PDF' }` na liście dokumentów do pobrania — obietnica bez pokrycia, w kontekście prawnym, na produkcji. Zakres: (1) regulamin publicznie dostępny (trasa `/regulamin` albo PDF w `static/` — decyzja przy realizacji); (2) odnośnik w stopce, widoczny **przed** zakupem, nie tylko po; ~~(3) zaślepka w panelu podmieniona na prawdziwy plik albo usunięta do czasu, aż będzie prawdziwa~~ **✅ 2026-07-31 (commit `9ef0c8f4`, na prod)**; (4) potwierdzić z Michałem, czy `.doc` z 07-07 to wersja finalna do publikacji.

  **Dopisek 08-10 (Michał):** przesłał `docs/assets/2026_08_09_SA_regulamin_rejsu_v2.doc` — wersja z progiem zwrotu przesuniętym na 45 dni (patrz **LEGAL-8**, do zweryfikowania zanim ten plik zastąpi stary `.doc` z 07-07 jako źródło prawne). Regulamin ma też dostać klauzulę o zmianie trasy z powodu pogody/awarii jachtu — Michał: „wydawało mi się że to wpisywałem", do sprawdzenia w v2. **✅ 2026-08-13 — potwierdzona jako finalna** (progi poprawione, patrz LEGAL-8); przeniesiona do `docs/business-decisions/2026_08_09_SA_regulamin_rejsu_v2.doc` (stary `.doc` z 07-07 zostaje obok jako historia, nie źródło prawne). Publikacja (punkty 1-2: trasa `/regulamin`, link w stopce) nadal nierobiona.

  **Punkt 3 — co się okazało przy realizacji:** zaślepką były **wszystkie cztery** pozycje listy `docs` w `dashboard/+page.svelte`, nie tylko regulamin — cała zakładka „Dokumenty" była makietą, która pojechała na produkcję. Przyciski były `disabled`, więc martwego linku nie było; nieprawdą były same wiersze (nazwa dokumentu + zmyślona data `01.01.2026`). Jedyny realnie istniejący plik to potwierdzenie rezerwacji (`/api/booking-confirmation/[bookingRef]`, dostępne już w zakładce „Rezerwacja"). Decyzja Tomka: zostawić tylko to, co istnieje, bez kolumny z datą. `docs` przepisane na `$derived` z `confirmationDocUrl` (jedna pozycja albo pusta tablica) + pusty stan; wiersze zmienione z `<button disabled>` na `<a href>`, bo pobranie to nawigacja. Trzy zmyślone nazwy dokumentów wypadły z katalogów `.po` do `#~`. Przy okazji zunifikowany warunek „potwierdzenie jest do pobrania" w jedną wartość `confirmationDocUrl` (URL albo `null`) zamiast pary URL + boolean — boolean nie zawęża typu w TypeScripcie, więc rozdzielenie tych dwóch faktów wymuszałoby asercję. Różni się od LEGAL-5, która pyta „czy formularze linkują do regulaminu" — tutaj problem jest wcześniejszy: **nie ma do czego linkować**. Rodzina fałszywych afordancji: badge „Valid", „Ostatnie miejsca". *(Tomek 07-30)*
- **LEGAL-7 — Profil organizatora: formalna nazwa, identyfikacja przedsiębiorcy, numer uprawnień, jedno źródło danych kontaktowych.** Trzy warstwy, wyszły przy INFRA-9. **(a) Dane kontaktowe** — `tel:+48601671182` i `mailto:sailingarchitects@gmail.com` wpisane ręcznie w **pięciu** miejscach: `site-footer`, `book:805-806`, `crew-guide-page` (dwa razy), od 07-31 (II) także `dashboard:487`. Zmiana numeru wymaga pięciu edycji, a pominięcie jednej nie da żadnego sygnału. **(b) Formalna nazwa i identyfikacja podmiotu** — dziś nie ma jej nigdzie; stopka podaje kontakt, ale nie mówi, kto sprzedaje. **(c) Numer uprawnień** — organizacja odpłatnych rejsów wchodzi w reżim organizatora turystyki. **Czy i jaki wpis jest wymagany, to pytanie do Michała i jego prawnika, nie do nas** — nasza część zaczyna się, gdy poda dane: gdzie mają być widoczne i skąd czytane. ~~**Część techniczna jest niezależna od odpowiedzi prawnej i można ją zrobić wcześniej:** `src/lib/data/organizer.ts` jako jedno źródło, zaciągane przez stopkę, panel, `/book`, przewodnik i szablony maili.~~ **Przepisane 2026-08-02 (Tomek): (a) nie jest niezależne i nie robimy pliku statycznego.** Powód: kontakt wygląda na jeden fakt tylko dlatego, że Michał jest jednocześnie **skiperem** i **podmiotem sprzedającym**. To dwie encje, obie docelowo w bazie i obie edytowane przez Michała — skiperów będzie więcej niż jeden. Plik `organizer.ts` utrwaliłby nazwą zlepek dwóch ról, a konsumenci i tak przepisaliby się drugi raz przy przejściu na bazę (odczyt stałej vs subskrypcja z trzema stanami) — zysk połowiczny, cel zły. **Nowy kształt pozycji:** (a) łączy się z (b) i (c) w jeden refaktor „profil skipera + podmiot sprzedający w bazie + panel edycji dla Michała"; punktem wejścia jest schemat, nie plik stałych. Blokada bez zmian: dane formalne po stronie Michała. Rodzina: [[one-instance-hides-two-entities]], REFACTOR-6 p.5, FEAT-11. Łączy się z LEGAL-1/2 (regulamin, polityka) i LEGAL-6 (publikacja regulaminu). *(Tomek 07-31 (II), przy INFRA-9; przepisane 08-02)*

  **Odpowiedź Michała 08-10 na (b):** nazwa firmy NIE ma być publikowana — na stronie zostaje wyłącznie kontakt: e-mail, telefon, osoba kontaktowa „Michał Smolarski". Oficjalna forma prawna: **prywatny rejs niekomercyjny składkowy** (nie działalność komercyjna) — redefiniuje pytanie (c) o numer uprawnień organizatora turystyki, bo reżim prawny bywa inny dla rejsu składkowego niż dla komercyjnej imprezy turystycznej; nadal pytanie do prawnika Michała. Ta decyzja o formie prawnej stoi też za falą zmian językowych na stronie — patrz **UI-17**. **Spisane jako ADR-021** (`docs/business-decisions/ADR-021-private-non-commercial-membership-cruise.md`, 08-13).
- **LEGAL-5 — Audyt odnośników do regulaminu i polityki prywatności we wszystkich punktach zbierania danych.** Przejść po kolei: formularz checkoutu, formularz pod linkiem z tokenem (`/crew/confirm/[token]`), formularz danych uczestnika w panelu — czy każdy ma widoczny odnośnik do regulaminu i polityki. To samo dla treści maili wychodzących (potwierdzenie, monity, prośba o dane, PDF). Gdzie brakuje — dodać. Blokowane przez LEGAL-1/LEGAL-2 (nie ma jeszcze do czego linkować), ale sam audyt „gdzie zbieramy dane" można zrobić wcześniej. *(Michał 07-20 dopisek #3)*
- ~~**LEGAL-8 — Progi zwrotów: opis Michała (feedback 08-10) nie zgadza się z żywą konfiguracją.**~~ **✅ 2026-08-13 — Michał potwierdził: 180/90/60/45 dni (100/80/50/0%) zostaje, regulamin v2 poprawiony do tych liczb (Tomek).** *(pierwotny opis:)* Michał 08-10: „2 te progi są ok, zmodyfikowałem ostatni na 45 dni" i podał tabelę: X-180=100%, 179-90=80%, 89-45=50%, 44-0=0%. Sprawdzone na żywo (`npx convex run refunds:getActiveRefundPolicy --prod`, 2026-08-13): aktywna polityka ma **cztery** progi, nie trzy z jego opisu — `180→100%`, `90→80%`, `60→50%`, `45→0%`. Efekt: rezerwacja odwoływana 45-59 dni przed wypłynięciem dostaje w systemie **0%**, a z tabeli Michała wynikałoby **50%** (jego opis nie wspomina progu 60 dni). Niejasne czy to literówka w wiadomości, czy realna rozbieżność do poprawienia w `/admin/automation`. **Nie zmieniać samodzielnie — potwierdzić z Michałem dokładne cztery liczby przed publikacją regulaminu v2** (LEGAL-6), żeby regulamin i system mówiły to samo. To już realne pieniądze — Stripe live od 08-13. **Rozbieżność trójstronna, nie dwustronna:** sprawdzony też tekst `regulamin_rejsu_v2.doc` (pkt 8) — ma **jeszcze inne** liczby niż obie powyższe: do 6 mies.=100%, 3-6 mies.=**90%**, 6-12 tyg=50%, ≤6 tyg (**42 dni**)=0%. Czyli regulamin v2 nadal ma stare 90%/42d z ADR-020, mimo że przesłany jako „po aktualizacji" — aktualizacja nie objęła tego akapitu. **Wyjaśnione 08-13: cztery progi live (180/90/60/45) wpisał ręcznie Tomek dziś rano w `/admin/automation`**, nie Michał — więc żadne z trzech źródeł nie jest z automatu „tym prawdziwym"; wszystkie trzy do uzgodnienia z Michałem naraz, jedną rozmową.

  **Decyzja Tomka 08-13: system zostaje jak jest (najkorzystniejszy dla Michała), regulamin v2 poprawiamy do tych liczb, nie odwrotnie.** Sprawdzone matematycznie: live (180d=100%, 90d=80%, 60d=50%, 45d=0%) daje refund % **≤** obu pozostałych źródeł w każdym przedziale dni (regulamin v2 miał 90% zamiast 80% dla 90-179d i 50% zamiast 0% dla 45-59d — oba na niekorzyść Michała). Zamiana w `.doc` z opisu miesiące/tygodnie (niejednoznaczne — „6 miesięcy" = 28-31 dni?) na dni: `180/100%, 90-179/80%, 60-89/50%, ≤59/0%`. Blokada: Michał musi kiwnąć głową, zanim regulamin v2 stanie się publiczną wersją (jego nazwisko/kontakt na dokumencie) — sama treść zmiany gotowa, brakuje tylko jego potwierdzenia. *(Tomek 08-13)*
- **LEGAL-9 — Checkout: brak checkboxa akceptacji regulaminu + zawarcia umowy uczestnika.** Michał 08-10: przy zakupie powinno być pole do odznaczenia, że kupujący zapoznał się i akceptuje regulamin rejsu oraz zawiera umowę uczestnika (dokumenty przesyła potem organizator z podpisem). Blokowane przez **LEGAL-6** (regulamin musi mieć publiczny URL, żeby był do czego linkować) i wymaga treści **umowy uczestnika** — nowy dokument `docs/assets/2026_08_sailing_architects_UMOWA UCZESTNIK_składkowy.odt` (feedback 08-10), do przeniesienia do `docs/business-decisions/` po potwierdzeniu wersji finalnej (ta sama konwencja co regulamin). Łączy się z LEGAL-5 (audyt odnośników do regulaminu) — checkbox to nowy punkt zbierania zgody, wejdzie do tego samego audytu. *(Michał 08-10, striaż Claude 08-13)*

## 🔐 Security / dane wrażliwe

- **SEC-1 — Szyfrowanie pól wrażliwych + minimalizacja/retencja.** `bookingParticipants.documentNumber` (nr dowodu/paszportu) w Convex jako szyfrogram (AES-256-GCM), klucz w env Vercela — **poza Convexem** (wyciek dumpa bazy = bełkot; szyfrowanie pola jest tak dobre jak separacja klucza od bazy). Granica szyfr/deszyfr = SvelteKit server, NIE klient (klucz w przeglądarce = problem dystrybucji; serwer i tak musi widzieć plaintext: PDF, manifest, maile). Konsekwencja: zapis wrażliwych pól z klienta musi przejść przez endpoint SvelteKit + `internalMutation` (wzorzec z `/api/admin/refunds/*`), dziś idzie prosto do `upsertBookingParticipant`; tracimy indeksy po zaszyfrowanym polu (nie szukamy po nim). NAJPIERW minimalizacja: czy pole musi istnieć i jak długo — retencja po rejsie + okresie roszczeń → kasować. Haszowanie NIE (jednokierunkowe — dane trzeba odczytywać). *(dyskusja Tomek 07-13, kontekst: handoff sesja 07-12/13)*
- **SEC-2 — Region EU Convex ✅ potwierdzony / zostaje: DPA + nota kosztowa.** (a) ✅ 07-13: prod `qualified-crab-196` = **Europe (Ireland)**, URL `eu-west-1.convex.cloud` — projekt tworzony z default Europe, **zero migracji, „dane w Europie" prawdziwe od startu**. Nota kosztowa na przyszłość: na darmowym planie zużycie EU liczy się do zwykłych limitów; **po upgrade na płatny plan** limity wliczone obejmują tylko US — EU billowane on-demand od pierwszego calla **+30% surcharge** (info dla Michała przy skalowaniu). (b) Zostaje: DPA od Convex, Clerk, Stripe, Brevo (wiąże się z LEGAL-2); uwaga: Clerk/Stripe/Brevo hostują w USA — „dane w Europie" dotyczy bazy Convex, nie całego łańcucha (auth, płatności, maile); w polityce prywatności opisać uczciwie. *(dyskusja Tomek 07-13)*
- **SEC-3 — Backup produkcyjnego deploymentu Convex.** ~~„No backup yet"~~ pierwszy backup ręczny zrobiony + pobrany 07-13 (Tomek). Plik eksportu zaszyfrowany (zip -e) 07-13; FileVault na Mac mini OFF — do włączenia przez Tomka. **Ustalony kierunek (07-13):** Warstwa 1 = snapshoty w Convex Backup & Restore (plik nie opuszcza chmury; sprawdzić harmonogram na darmowym vs Pro). Warstwa 2 = cykliczny (np. miesięczny) eksport szyfrowany PRZED wysyłką (`zip -e`, jedno mocne hasło w menedżerze haseł) → iCloud Drive (opcjonalnie Advanced Data Protection dla E2E); retencja 3 ostatnie; poza Downloads i poza repo. Kwartalnie test odtworzenia: `npx convex import` na dev. *(wykryte 07-13 przy weryfikacji regionu)*

- **SEC-5 — Brak śladu zmian danych osobowych uczestnika.** Pytanie Tomka 07-31 (II): gdy Michał zgłosi załogę i jej dane, a uczestnik potem zmieni numer dokumentu, zgłoszenie przestanie odpowiadać bazie — czy gdziekolwiek to widać? **Nie widać.** `upsertBookingParticipant` robi `ctx.db.patch` i nic więcej: stare wartości nadpisane, nie do odzyskania, zero wpisu w logu. `adminAuditLog` ma sześć akcji (`refund_initiated`, `refund_completed`, `refund_failed`, `reblock_berth`, `release_berth_manual`, `policy_updated`) — same pieniądze i koje, **danych osobowych nie loguje w ogóle**. Edycja przez admina zostawia `adminEditedAt`/`adminEditedBy`, ale to stempel ostatniego pisarza, nie historia. Działa jeden mechanizm spójności w drugą stronę: edycja admina zrzuca `confirmationStatus` na `drafted_by_admin`, więc uczestnik musi zatwierdzić ponownie. **Kierunek (wzorzec ten sam co snapshot polityki zwrotów w ADR-002):** zgłoszenie do urzędu to fakt historyczny, rekord uczestnika to stan bieżący — jedna tabela do obu ról zawsze skłamie po którejś stronie. Docelowo: (1) snapshot zgłoszenia (co poszło, niezmienne), (2) log zmian źródła (kto, kiedy, z czego na co), (3) widok różnicy („3 pola zmienione od zgłoszenia"). Blokada po zgłoszeniu jest wariantem gorszym — żeglarz z literówką w numerze dowodu i tak zadzwoni do Michała, czyli wraca ręczna robota, którą panel miał zdjąć. **Poprzedza to pytanie do Michała: czego dokładnie wymaga urząd, komu się zgłasza i czy dopuszcza korektę po złożeniu** — bez tego nie wiemy, czy zmiana po zgłoszeniu jest kłopotem operacyjnym (kapitan ma nieaktualną listę) czy formalnym. Łączy się z SEC-1 (szyfrowanie pól wrażliwych), LEGAL-4 (retencja) i FEAT-3 (rozbudowa audit logu). *(Tomek 07-31 (II), przy INFRA-9)*

## 🎨 UI / landing (drobne, spoza Michał 06-19)

- **UI-1 — Mobile: odwrócić kolejność CTA** po „Rezerwuj" — najpierw „Zaloguj się do panelu", potem „Rezerwacja jako manifest pokładowy". *(feedback 07-05 #3)*
- **UI-2 — Wolne/zajęte miejsca wyraźniej** w layout strony. *(Michał 07-07 #5)*
- **UI-3 — Podciągnąć wyrazistość/percepcję strony i panelu** (Michał: obrazy `assets/WhatsApp Image 2026-07-07 at 15.08.44/15.09.09.jpeg`). *(Michał 07-07 #8)*
- **UI-4 — Admin: mobile-tabs rozjeżdżają się przy szerokości ~1180px.** Breakpoint chowa sidebar, a taby sekcji renderują się jako ogromne kafle na szerokości tabletu — layout „do kitu" między desktopem a telefonem. Przejrzeć media query w `admin/+layout@.svelte` (breakpoint + wysokość/proporcje `.mobile-tabs`). *(Tomek 07-15, zauważone przy BUG-6)*
- **UI-5 — Admin: nie da się wyczyścić pola uczestnika w drawerze.** Puste pole → `optional()` → `undefined` → Convex traktuje jako „argument nieobecny" → `ctx.db.patch` nie dotyka pola, stara wartość zostaje. Puste = „nie ruszaj", nie „wyczyść". Fix wymagałby zmiany kontraktu mutation (rozróżnienie brak-argumentu vs ustaw-pusto); najpierw decyzja, czy czyszczenie pól to realny use-case admina. *(wykryte 07-16 przy BUG-7)*

- **UI-6 — `font-size: 9px` na badge'ach i etykietach landingu.** Badge dostępności (`card__badge`, `segments__badge`) dziedziczy 9px z istniejącego języka wizualnego strony — poniżej progu czytelności dla tekstu niosącego informację handlową („Wyprzedane", „Wolne N miejsc"), zwłaszcza na mobile i przy dysleksji. Kontrast jest OK (zmierzone 07-25: 6,2:1 złoty, 10,5:1 neutralny) — problem dotyczy wyłącznie rozmiaru. Nie ruszać punktowo: to samo 9px siedzi w eyebrow'ach i etykietach całego landingu, zmiana jednego miejsca da niespójność. **Wziąć przy FEAT-5** (przebudowa landingu pod wiele rejsów) jako przegląd skali typograficznej. *(obserwacja Claude 07-25, decyzja Tomka: łączymy z FEAT-5)*

- ~~**UI-7 — Mobile: CTA rezerwacji ukryte w hamburgerze.**~~ ✅ 2026-07-27 (commit `30aa674d`, na prod). Nowy kontener `.mobile-actions` obejmuje CTA + hamburgera; `display: none` na desktopie (żeby `space-between` w `.site-nav` dalej widział dwoje dzieci i nie odklejał `.cluster` od prawej), `display: flex` w media query ≤900. Duplikat przycisku usunięty z rozwijanego menu. Etykieta dostała ` ` przed strzałką — przy 375px łamała się tak, że strzałka zostawała sama w drugiej linii. Zmierzone 375/320: brak poziomego scrolla. Ogony: **UI-12**, **UI-13**. *(pierwotny opis: logika etykiety istnieje od 07-14 i renderuje się w obu miejscach, ale na mobile siedziała wewnątrz hamburgera)*
- ~~**UI-8 — Mobile: pasek statystyk w hero ucina czwartą kolumnę.**~~ ✅ 2026-07-27 (commit `24716b59`, na prod). **Opis w backlogu był błędny** — mówił o „zawijaniu w zakładkach etapów" (`cabins-section`); screenshot Michała i reprodukcja pokazały pasek `.stats` w hero (`+page.svelte`), gdzie kolumna „41 dni / ŁĄCZNIE" wychodziła poza ekran. Przyczyna: `.stats__cell` z `min-width: 90px` × 4 + gapy = 363px podłogi przy 335px dostępnych — flex nie ściska poniżej `min-width`, więc wypycha. Fix w media query ≤720: `.stats { width: 100% }`, `.stats__cell { flex: 1; min-width: 0; padding: 10px 6px }`, `.stats__value { font-size: min(22px, 5vw) }` + ukrycie kolumny „etapy" (`:nth-child(1)`, z komentarzem) jako powtórzenia leadu widocznego 100px wyżej. Zmierzone 320/360/375/720: zero przepełnienia (`scrollWidth === clientWidth`). *(Michał 07-25 #1)*
- ~~**UI-9 — Mobile: po wyborze segmentu przenieść widok do planu jachtu.**~~ ✅ 2026-07-30 (commit `037fcff0`, na prod). Reprodukcja potwierdziła opis: przy 360px plan jachtu startuje 588px od góry, klik w zakładkę nic nie przewijał. Fix w `cabins-section.svelte`: wrapper `<div class="plan-anchor" bind:this={planEl}>` wokół `<BoatPlan>` (`bind:this` na komponencie zwraca instancję, nie DOM node) + `scrollIntoView` w `selectSegment`. **Warunek na widoczności, nie na breakpoincie:** `rect.bottom > window.innerHeight` zamiast `matchMedia` — nie powiela liczby z CSS i łapie też niskie okno na desktopie. Pierwsza wersja (`rect.top > innerHeight / 2`) przewijała na desktopie przy krótkim planie leżącym nisko — „gdzie się zaczyna" ≠ „czy się mieści". Etap wyprzedany pomijany przez `freeBySlug.get(id) === 0` (definicja z `berth-badge.ts:9`, nie przez funkcję prezentacji), guard obejmuje **wyłącznie** scroll — przełączanie zakładki działa dalej. `scroll-margin-top: 64px` na wrapperze, żeby dziób nie chował się pod sticky navem. *(Michał 07-25 #2)*
- ~~**UI-14 — Panel żeglarza: nieaktywne zakładki wyglądają na wyłączone.**~~ ✅ 2026-08-05 (commit `83cdb043`, na prod). Zmierzone: nieaktywna zakładka **3.46** przy progu AA 4.5. Naprawa nie punktowa — powstała skala tokenów w `app.css` (`--color-on-surface` / `-soft` / `-muted`), zakładka dostała `-soft` (7.97) zgodnie z regułą **element interaktywny nie schodzi poniżej treści, którą obsługuje**. Przy okazji wyszło, że hover (0.7) zrównał się ze spoczynkiem po podniesieniu — rozdzielony do pełnej bieli. Powiązane: [[interactive-states-are-differences]], [[contrast-is-a-property-of-the-pair]]. *(pierwotny opis:)* Pasek zakładek („REZERWACJA / DANE ZAŁOGI / DOKUMENTY" w `dashboard/+page.svelte`) — aktywna ma złoty tekst i podkreślenie, nieaktywne szary tekst na ciemnym tle. Kontrast **między stanami** jest dobry; problemem jest czytelność stanu nieaktywnego samego w sobie: wygląda jak `disabled`, a nie jak klikalny. Ta sama rodzina co lekcja z 07-25 — **barwa niesie znaczenie, kontrast niesie ważność** — tu stan „dostępne, ale nieaktywne" dostał kontrast zarezerwowany dla „niedostępne". Wchodzi razem z **UI-11** (przegląd kontrastu całego projektu), bo to ten sam rodzaj decyzji na palecie, nie punktowa poprawka. *(Tomek 07-31, przy LEGAL-6 p.3)*
- **UI-15 — Hero: „41 dni ŁĄCZNIE" czyta się jako suma etapów, a suma etapów to 38.** Znalezione przy INFRA-9. Pasek `stats` w `+page.svelte:10` podaje cztery liczby wpisane ręcznie: `4 etapy`, `10 miejsc`, `~2200nm`, `41 dni`. **Żadna nie jest fałszywa** — i żadna nie jest wyliczana. Problem jest w czwartej: 41 to rozpiętość dat (4.10 → 14.11), a suma pól `days` z `voyage-segments.ts` wynosi 38 (7+9+9+13). Obie prawdziwe, mierzą co innego — 38 to czas na wodzie, 41 to czas trwania wyprawy. Różnica trzech dni to **postoje w porcie na zmianę załogi**, fakt nigdzie w projekcie nie zapisany, ujawniony wyłącznie jako niezgodność dwóch liczb. Kupujący, który zsumuje etapy z cennika, dostanie inny wynik niż z hero i nie ma jak rozstrzygnąć, która strona kłamie. **Naprawa, od najtańszej:** (1) doprecyzować etykietę („41 dni · cała wyprawa"); (2) pokazać obie miary, jeśli obie ważą przy decyzji zakupowej; (3) wyliczać z jednego źródła. **Czego nie robić: podmienić 41 na 38 „dla spójności"** — to przeniesienie nieporozumienia w drugą stronę. Źródła pozostałych trzech: `4` = liczność `voyageSegments`, `10` = `cabins.ts` (5 kabin × 2), `~2200nm` = **brak, tylko Michał** (patrz INFRA-10). Kopie ręczne wchodzą do REFACTOR-6. Powiązane: [[two-measures-one-label]]. *(Tomek/Claude 07-31 (II), przy INFRA-9)*
- **UI-11 — Przegląd czytelności całego projektu (kontrast tło↔czcionka).** Michał na własnym telefonie: „nie patrzy się na niego dobrze". Sprawdzić wszystkie pary tło/tekst — landing, panel żeglarza, admin — czy różnice są wystarczające, ze szczególnym uwzględnieniem tekstu na zdjęciach (hero) i warstw z `alpha` (kontrast liczy się po zmieszaniu warstw). **Zrobić przed przebudową landingu (FEAT-5)**, żeby nowy layout startował ze zdrowej palety, a nie dziedziczył problem. Łączy się z UI-3 (wyrazistość) i UI-6 (9px w typografii). Screenshot: `docs/assets/IMG_3269.jpeg`. *(Michał/Tomek 07-26 #2)*
  - **Postęp 2026-08-05** (commity `83cdb043`, `15f3ab9b`, `7de6b4bf`, `d564d1cd`, na prod). **Zrobione:** skala tokenów w `app.css` z podłogą zmierzoną (`-muted` = alfa 0.5 = 4.69 na `--color-navy`); 26 literałów podmienionych na tokeny w 10 plikach (najgorszy: alfa 0.2 w stopce, 1.74); trzy naprawy stanów hover (mapa przygaszała zamiast rozjaśniać, wybór etapu w kabinach miał `transition` bez reguły `:hover`, `.auth-tab` spoczywał pod progiem); gradienty hero przestrojone + osobna warstwa dla `max-width: 720px`. **Narzędzie:** `docs/tools/contrast-audit.js` — wklejany w konsolę audyt DOM, z listą ograniczeń w nagłówku. **Zostaje:** panel żeglarza i admin za logowaniem (audytor tam nie wchodzi bez sesji), stany `:hover`/`:focus` w całym projekcie, treści pojawiające się po interakcji. Odłożone świadomie 08-05 — praca mechaniczna i męcząca, wracamy w osobnej sesji. Powiązane: [[audit-script-and-eye-see-different-things]].
- **UI-16 — Tekst nad zdjęciem hero i przezroczysty nav nie osiągają AA.** Wyszło przy UI-11 (08-05) i **nie da się tego naprawić od strony tekstu** — sufit zmierzony: `.subtitle` przy pełnym mosiądzu daje 4.49 desktop / 4.25 mobile, linki nawigacji przy pełnej bieli 3.93 na jasnym niebie (próg 4.5). Przyczyna: kolor o średniej luminancji na tle o zmiennej jasności; nav przed przewinięciem nie ma własnego tła (`background: transparent`). Pozostałe środki są **wizualne, nie techniczne**: scrim pod kolumną tekstu w hero (widoczny prostokąt na fotografii) albo przyciemnienie paska nawigacji także w stanie początkowym. Do rozstrzygnięcia z Michałem, najlepiej razem z FEAT-5. Powiązane: [[text-over-photo-has-a-ceiling]]. *(Tomek/Claude 08-05, przy UI-11)*
- **REFACTOR-7 — Trójstan zakładek wpisany od nowa w czterech miejscach.** Ten sam układ `btn` / `:hover` / `--active` (przygaszony spoczynek, jaśniejszy hover, mosiądz + podkreślenie dla wybranego) stoi osobno w `dashboard/+page.svelte` (`.tabs__btn`), `route-section.svelte` (`.selector__btn`), `cabins-section.svelte` (`.segments__btn`) i `book/+page.svelte` (`.auth-tab`). Każda kopia psuła się osobno i każdą trzeba było naprawiać osobno przy UI-11 — trzy z czterech miały zepsuty albo brakujący hover. Kandydat na jeden komponent zakładek; wchodzi z FEAT-5, nie osobno. Powiązane: [[interactive-states-are-differences]]. *(Claude 08-05, przy UI-11)*
- ~~**UI-12 — Etykieta CTA ma zmieniać się także dla niezalogowanego.**~~ ✅ 2026-07-30 (commit `8348d1f7`, na prod). **Michał potwierdził zmianę** — odejście od jego literalnego feedbacku z 07-17 zaakceptowane. Człon `isSignedIn` usunięty z `reserveLabel`; łańcuch martwego kodu pociągnięty do końca: bez konsumenta zostały `isSignedIn`, potem `ctx`, potem import `useClerkContext` — wszystkie trzy usunięte (1 linia dodana, 5 usuniętych). Sprawdzone wylogowany i zalogowany. *(pierwotny opis:)* Dziś `reserveLabel` (`site-nav.svelte:41-45`) wymaga `hasSelectedBerths && isSignedIn`, więc gość z wybraną koją widzi „Rezerwuj" zamiast „Kontynuuj rezerwację →". Decyzja Tomka 07-27: sam wybór koi ma wystarczyć — usunąć człon `isSignedIn`. **Uwaga: to odejście od literalnego feedbacku Michała z 07-17** („zalogowany AND koje wybrane"), warte potwierdzenia z nim. Zmiana jednoliniowa; logowanie i tak jest częścią dalszego kroku checkoutu, więc etykieta pozostaje prawdziwa. *(Tomek 07-27)*
- ~~**UI-13 — Rozjazd spacji w etykiecie „Kontynuuj rezerwację →" między nav a `/book`.**~~ ✅ 2026-07-30 (commit `0cde590e`, na prod). `book/+page.svelte:758` dostał spację nierozdzielającą jak nav. Po ekstrakcji wuchale stary `msgid` przeszedł w `#~` w obu katalogach, nowy zbiera oba odnośniki źródłowe — jeden wpis dla tłumacza zamiast dwóch bliźniaków. Sprawdzone, że nic nie zginęło: stary `msgstr` w `en.po` był pusty już wcześniej (**zmiana literału = nowy klucz = utracone tłumaczenie**, gdyby jakieś było). Rozstrzygnięcie wymagało porównania bajtów (`repr()` wycinka przed strzałką) — `grep` i oko nie odróżniają tych dwóch znaków. Powiązane: [[identical-looking-strings-compare-bytes]]. *(pierwotny opis:)* Rozjazd spacji w etykiecie „Kontynuuj rezerwację →" między nav a `/book`.** UI-7 wstawił w `site-nav.svelte` spację nierozdzielającą (` `) przed strzałką, żeby przy 375px nie zostawała sama w drugiej linii. Ten sam napis w `routes/[[lang=lang]]/book/+page.svelte` ma nadal zwykłą spację → ta sama sierota może wystąpić tam. Skutek uboczny: w `.po` są **dwa `msgid` wyglądające identycznie**, różniące się jednym bajtem — mylące dla tłumacza. Fix: ujednolicić na ` ` i zamknąć starszy klucz. *(Tomek/Claude 07-27, przy UI-7)*
- **UI-17 — Przepisanie języka strony na niekomercyjny/składkowy.** Konsekwencja decyzji o formie prawnej „prywatny rejs niekomercyjny składkowy" (LEGAL-7, Michał 08-10). Konkretne zamiany, ze screenshotami w `docs/assets/WhatsApp Image 2026-08-09 at 00.*.jpeg`: (1) w menu i wszędzie na stronie „cena" → „składka"; (2) „cena za miejsce" → „Składka za osobę"; (3) „przejdź do płatności" → „Opłać składkę"; (4) „wyprzedane" → „brak miejsc" (dotyka **FEAT-8**, `src/lib/berth-badge.ts`); (5) tagline „Boutique sailing trips" → wariant z „private" (string widoczny w `en.po`/`pl.po`, znaleźć źródło w kodzie); (6) nowy slogan na samej górze strony: „Zapraszamy do załogi na nasz prywatny rejs towarzysko szkoleniowy" (pozycja pokazana na screenshocie `00.18.26`); (7) przegląd całej strony pod kątem innych sformułowań brzmiących komercyjnie — Michał: „jak jeszcze zauważysz coś sugerującego komercję, co przeoczyłem, to też zmień" — subiektywne, przejrzeć razem z Michałem, nie automatem. Dotyka i18n (`.po` pliki) — zmiana literału = nowy klucz tłumaczenia, stary `msgstr` osierocony, patrz [[identical-looking-strings-compare-bytes]]. *(Michał 08-10, striaż Claude 08-13)*
- **UI-18 — `/admin/automation`: przycisk zapisu na górze karty, nie na dole edytowanej listy.** Tomek 08-13, przy ręcznej zmianie progów zwrotów: kliknął **„Zapisz plan"** (góra karty „Plan dla segmentu") mając na myśli **„Zapisz politykę"** (góra karty „Polityka zwrotów", bezpośrednio pod spodem) — dwa wizualnie identyczne złote przyciski, stackowane pionowo blisko siebie, różniące się jednym słowem, oba na górze swojej karty zamiast pod ostatnim wierszem który edytujesz. Naturalny przepływ: edytuj wiersze (środek/dół karty) → zapisz (od razu pod nimi); dziś trzeba wracać wzrokiem/kursorem w górę, mijając przycisk sąsiedniej karty po drodze. `src/routes/[[lang=lang]]/admin/automation/+page.svelte`, karty „Plan dla segmentu" i „Polityka zwrotów". Kierunek: przenieść „Zapisz…" pod listę wierszy (koniec karty) zamiast obok „Generuj pozycje"/„Dodaj próg" na górze; przy krótkich listach (3-4 wiersze) strata przycisku „na widoku" bez scrolla jest bez znaczenia. *(Tomek 08-13, przy weryfikacji progów LEGAL-8)*
- **UI-10 — `/admin/audit`: kolumna „admin" pokazuje identyfikator, nie człowieka.** Audit log zapisuje `adminEditedBy` / autora akcji jako `subject` z Clerka (nieczytelny ciąg). Michał chce formy przyjaznej — imię/nazwisko albo e-mail operatora. Do rozstrzygnięcia: mapować przy odczycie (query dociąga profil po `subject`) czy denormalizować przy zapisie (snapshot nazwy w wierszu audytu — historia nie kłamie po zmianie nazwy konta; ta sama decyzja co przy snapshotach polityki zwrotów). Łączy się z FEAT-3 (rozbudowa audit logu). *(Michał 07-20 dopisek #1)*

## 🎨 Landing — uwagi Michała 2026-06-19 (22 poz.)

→ **Szczegóły: `handoff.md` sekcja „Backlog Michała — landing (2026-06-19)".** ~~13 pozycji copy~~ ✅ 2026-07-12 (commit `9a02312e`). ~~Ramka logo~~ ✅ 07-13 (`ff60d02c`). ~~Mapa: realna geografia + mobile~~ ✅ 07-13 (`875e731a`). ~~Link Instagram~~ ✅ 07-13 (`462e492c`, `instagram.com/sailing_architects`). **Zostają tylko:** galeria (Later — Michał odłożył), zakładka „O nas" (Later), blok „nikt nie wie o co chodzi" (czeka na autora); PDF itinerary = FEAT-1. Otwarte z copy: czy „Wachty nawigacyjne" zostaje osobno obok „aktywnego udziału załogi" (pytanie do Michała).

## ⚙️ Post-MVP admin (świadomie odłożone, ~33 poz.)

→ **Szczegóły: `docs/admin-post-mvp-decisions.md`** (format Stan/Trigger/Kierunek). Bierz gdy trigger realny (sprzedaż/feedback kapitana). Główne: granularne role kapitan/operator, eksport CSV Sales Board, nightly admin e-mail, konfigurowalne reguły monitów, single-source `voyageSegments`, `+layout@` reset na `/crew/confirm/[token]`, „Poproś o nowy link", WhatsApp/SMS API. **Uwaga: BUG-1/4/5/6/7 pochodzą z tego pliku — po naprawie skreśl w obu miejscach.**

## 📚 Nauka / jakość kodu

- ~~**LEARN-1 — Lekcja ekstrakcji: logika refundów do `_lib/` + testy.**~~ ✅ 2026-07-11 (sesja II) — `matchRefundTier` → `_lib/refundTiers.ts` (5 testów) + `allocateCascade` → `_lib/refundCascade.ts` (8 testów, throw ×3), handlery cienkie, commit `451e0c6a`, CI zielone.
- ~~**INFRA-1 — ESLint** (eslint + eslint-plugin-svelte). Trzeci brak z audytu 07-11 (po CI ✅ i testach ✅). Prettier ≠ linting: brak kontroli a11y, unused vars, wzorców Svelte.~~ ✅ 2026-07-12 — flat config (js+ts+svelte+prettier, ignores z `.gitignore`), triaga 54 znalezisk: 19 fixów (eqeqeq ×3, martwe inicjalizatory ×3, unused ×3, ctx `any`→`QueryCtx`/`DatabaseReader` ×6, prefer-const), 5 inline-disable z uzasadnieniem, wpięte w `pnpm lint` + CI.
- **INFRA-2 — `svelte/no-navigation-without-resolve` (23 warningi).** Przejście linków/`goto()` na `resolve()` z `$app/paths` — typowane trasy. Reguła zdegradowana do `warn` w `eslint.config.js`; refactor 23 miejsc (site-nav ×6, book ×6, admin ×5, reszta pojedyncze) jako osobna sesja.
- **INFRA-3 — Przegląd zależności w `package.json`.** Do sprawdzenia nowe/odstające pakiety *(Tomek 07-15)*. Konkretny sygnał z sesji 07-15: dryf svelte-clerk — typy `clerk-js` 6.7.7 w node_modules vs runtime 6.25.3 z CDN (źródło problemów z appearance i propsami; wiki `clerk-cdn-runtime-version-drift`). Rozważyć aktualizację svelte-clerk/@clerk i przegląd reszty (`pnpm outdated`).
- **INFRA-4 — Przenośność sekretów między maszynami (`.env`).** 07-19: przesiadka MacMini→MacAir = brak `.env`, brak testu lokalnego, deploy „w ciemno". Kierunek: sekrety dev trzymane w Vercel env (Development) → na nowej maszynie `vercel login && vercel link && vercel env pull .env`; zmienne Convex uzupełnia `npx convex dev`. Alternatywa: 1Password/iCloud jako sejf na plik `.env`. NIE commitować `.env` do repo (nawet zaszyfrowanego, dopóki nie ma sops/age flow). *(Tomek 07-19)*
- **INFRA-5 — Convex dev dostępny z MacAira (test po LAN).** Cel: appka + Convex chodzą na Mac mini, testowanie z MacAira przez przeglądarkę (`http://<ip-macmini>:5173`) bez spamu `ws://127.0.0.1:3210 failed 1006`. Przyczyna błędu: `PUBLIC_CONVEX_URL=http://127.0.0.1:3210` — `127.0.0.1` z perspektywy przeglądarki na MacAir = localhost MacAira, gdzie Convex nie chodzi. Kierunek: (1) wystawić Convex dev backend na `0.0.0.0` (sprawdzić flagę `npx convex dev` / local backend — czy w ogóle da się LAN-bind); (2) `PUBLIC_CONVEX_URL` na `http://<ip-macmini>:3210` (osobny `.env.local` dla trybu LAN, żeby nie mieszać z domyślnym localhost); (3) Vite już wystawiony (`host: true`). Uwaga: IP z DHCP potrafi się zmienić — rozważyć rezerwację/stały adres Mac mini. Do czasu tego: otwierać `localhost:5173` na maszynie z Convex. *(Tomek 07-24)*
- **INFRA-6 — Audyt całego kodu pod kątem testów jednostkowych.** Dziś testy istnieją tylko dla `_lib/refundTiers.ts` i `_lib/refundCascade.ts` (LEARN-1, 07-11). Przejrzeć repo i wypisać, co jest testowalne bez mocków (czyste funkcje, helpery `_lib/`, walidatory zod, formatery), co wymagałoby wydzielenia z handlerów, a co świadomie zostaje bez testów (I/O, komponenty). Wynik: lista priorytetów, nie „dopiszmy testy wszędzie". Kandydaci widoczni od ręki: `_lib/bookingClosed.ts`, schematy w `lib/schemas/`. (`_lib/berthFree.ts` odhaczone 07-26 — 7 testów.) *(Tomek 07-25)*
- **INFRA-7 — Komunikaty błędów z mutacji Convex nie docierają do klienta na prodzie.** Convex traktuje `throw new Error(...)` jako błąd deweloperski i **wycina treść** w deploymentach produkcyjnych — klient dostaje gołe „Server Error". Pełny komunikat widać wyłącznie w dev (potwierdzone 07-26 na `mutations.ts:868`, „Podaj poprawny adres e-mail"). Żeby treść przeszła do klienta, trzeba rzucić `ConvexError` (nośnik `data`); w repo nie występuje ani razu. Zakres do przejrzenia: wszystkie `throw new Error` w `src/convex/`, podział na (a) błędy przeznaczone dla użytkownika/admina → `ConvexError`, (b) sanity check dla dewelopera → zostaje `Error`. Priorytet niski: warstwa serwerowa to defense-in-depth, w normalnym przepływie zatrzymuje wcześniej walidacja kliencka z czytelnym toastem. *(wykryte 07-26 przy smoke teście BUG-7)*
- **REFACTOR-2 — Identyfikatory segmentów: jedno źródło prawdy.** `voyageSegments[].id` w statycznym `src/lib/data/voyage-segments.ts` (`'s1'..'s4'`) musi być identyczny ze `slug` w tabeli `voyageSegments` w Convex — nic tego nie pilnuje, zgodność utrzymywana ręcznie. Rozjazd = badge/ceny/statusy przypisane do złego etapu, po cichu. Ta sama rodzina co FEAT-11 (cena w 4 miejscach) — rozważyć wspólne rozwiązanie: dane segmentu wyłącznie z DB, statyczny plik znika albo staje się typem. *(wykryte 07-25 przy FEAT-8, gdy `listBerthAvailability` kluczowana slugiem trafiła do pickera kluczowanego `seg.id`)*
- ~~**REFACTOR-5 — `markOverduePayments` oznacza raty zaległymi, nie patrząc na rezerwację.**~~ **✅ 2026-08-06 (commit `184fdf01`, na prod, migracja uruchomiona na dev i prod).** Rozwiązane **nie tam, gdzie zakładała pozycja**. Diagnoza z backloga („dołóż odczyt bookingu w cronie, sprawdź koszt, rozważ odwrócenie kierunku skanu") prowadziła w ślepy zaułek: samo pominięcie wiersza (`continue`) nie usuwa go ze zbioru kandydatów, bo nic nie zapisuje — martwe raty zostałyby w indeksie na zawsze, gromadząc się na jego początku i zjadając `PAYMENT_SCAN_LIMIT`, aż cron przestałby dosięgać rat żywych, raportując przy tym `{ scanned: 200, updated: 0 }`. Właściwe miejsce naprawy to **`processStripeRefund`**: w momencie, w którym `paymentStatus` staje się `refunded` (jedyny writer tej wartości, `mutations.ts:1424`), wszystkie raty inne niż `paid` dostają `cancelled` — zgodnie z konwencją, którą stosowały już anulowanie rezerwacji i wygaśnięcie holdu. Wiersze w pamięci, zero dodatkowych odczytów; pytanie o kierunek skanu **zniknęło**, zamiast zostać rozstrzygnięte. Odrzucone kryterium `isBookingClosed` — wymaga koniunkcji „zwrócone **i** koje oddane", więc pełny zwrot bez zaznaczenia kwadracika zwolnienia koi przechodził przez obronę i monity szły dalej; decyzja Tomka: *„wysyłania nie możemy uzależniać od tego, co zrobi Michał"*. Reguła biznesowa (pełny vs częściowy zwrot) → **ADR-020**. Migracja `cancelPaymentsOfRefundedBookings` poprawiła 8 rat w 3 rezerwacjach na prodzie (przewidywanie z danych = wynik; powtórna sucha próba 0). **Punkt (2) świadomie niezrobiony:** obrona u odbiorców (`isBookingClosed` w dwóch cronach mailowych) **zostaje**, choć po tej zmianie jest praktycznie nieosiągalna — koszt trzymania to dwa odczyty na kandydata raz na dobę, koszt błędnego usunięcia to wezwanie do zapłaty wysłane po zwrocie. To nie przeoczenie, to asymetria kosztów. Poprzedni opis pozycji: [`reminders.ts:50`](../src/convex/reminders.ts) przestawiał `pending → overdue` wyłącznie po `dueAt`, nie czytając tabeli `bookings` ani razu. *(wykryte 07-26 przy BUG-8, zamknięte 08-06)*
- **SEC-4 — Ręczne wysyłki z drawera nie mają serwerowego guardu zamknięcia.** Akcje „Wyślij monit" / „Wyślij prośbę o dane" / dispatch potwierdzenia ([`crewConfirmation.ts:56`](../src/convex/crewConfirmation.ts)) sprawdzają istnienie bookingu, ale nie to, czy jest zamknięty. Chroni je wyłącznie `disabled={isClosed}` w [`booking-drawer.svelte`](../src/lib/components/admin/booking-drawer.svelte) — do obejścia z konsoli przeglądarki. Dokładnie ten sam kształt co BUG-7 (walidacja tylko po stronie klienta): normalny przepływ jest bezpieczny, brakuje drugiej warstwy. Priorytet niski (akcje wymagają uprawnień admina, więc atakującym byłby operator). *(wykryte 07-26 przy przeglądzie do BUG-8)*
- **REFACTOR-4 — `berths`: wykluczyć niespójne stany na poziomie schematu (discriminated union).** Dziś `holdExpiresAt`, `holdPaymentIntentId`, `bookingPaymentIntentId`, `guestName` są `v.optional()` niezależnie od `status`, więc baza przyjmie każdą kombinację — np. `held` bez `holdExpiresAt` (stan nieosiągalny przez aplikację, ale możliwy ręcznie z Dashboardu; wyprodukowany przypadkiem 07-26). Convex pozwala opisać tabelę jako `v.union(v.object(...), ...)` z `status` jako dyskryminatorem (`_generated/ai/guidelines.md` linia 43) — wtedy niespójny wiersz jest nie do zapisania, zamiast być stanem, przed którym broni się kod. Koszt: 5 kształtów (po jednym na status) z powtórzonymi polami wspólnymi + migracja widen-migrate-narrow wszystkich wierszy dev i prod. **Do sprawdzenia przed podjęciem:** czy indeks `by_status_and_hold_expires_at` działa, gdy `holdExpiresAt` nie występuje w czterech z pięciu wariantów. *(pomysł Tomka 07-26 przy REFACTOR-3)*
- ~~**REFACTOR-3 — Reguła „czy hold jeszcze obowiązuje": została ostatnia kopia.**~~ Predykat mieszka w `src/convex/_lib/berthFree.ts` (`isBerthFree`) i obsługuje obie query w `queries.ts` oraz — od 07-26 — KPI `heldBerths` w `admin.ts` (`status === 'held' && !isBerthFree(b, now)`; bezzmianowość zmierzona na `/admin`: hold aktywny → Held 1 przed i po, kontrpróba z holdem wygasłym → 0). **Zostaje [`mutations.ts:478`](../src/convex/mutations.ts)** (`typeof === 'number' && <= now`, guard sprzedaży w `createBooking`). **To NIE jest czysta podmiana** — kopia z `mutations.ts` różni się od helpera na jednym stanie: koja `held` **bez** `holdExpiresAt` jest dla guardu zajęta (nie do sprzedania), a dla `isBerthFree` wolna. Podstawienie helpera uczyniłoby taką koję sprzedawalną. Przed podejściem odpowiedzieć: czy `held` bez `holdExpiresAt` w ogóle powstaje (skąd), i czy taka koja ma być sprzedawalna, czy zablokowana do ręcznej interwencji. Decyzja biznesowa (ryzyko podwójnej sprzedaży) → kandydat na ADR, osobny commit. *(Tomek 07-25)* **✅ 2026-07-26 — zamknięte.** Ustalone dowodowo, że `held` bez `holdExpiresAt` nie powstaje przez aplikację (jedyny zapis `status: 'held'` w repo — `mutations.ts:527` — ustawia oba pola w jednym `patch`; wszystkie miejsca kasujące `holdExpiresAt` równocześnie wychodzą ze statusu `held`). Stan osiągalny wyłącznie ręcznie → REFACTOR-4. Decyzja: **ostrożniejsza reguła guarda wygrywa i staje się wspólna** (Tomek: „lepiej nie sprzedać niż sprzedać podwójnie"), nie odwrotnie — `isBerthFree` traktuje teraz `held` bez liczbowego `holdExpiresAt` jako zajętą, przez co `mutations.ts` stało się czystą podmianą (`if (!isBerthFree(berth, now)) throw`), identyczną na wszystkich stanach. Uboczna zmiana odczytu badge'a i KPI dotyczy wyłącznie stanu nieosiągalnego. Dowód: `src/convex/_lib/berthFree.test.ts`, 7 przypadków.
- **REFACTOR-6 — Dane etapów rejsu żyją w czterech kopiach.** Znalezione przy INFRA-9. Te same fakty (nazwy, daty, ceny, długości etapów) leżą w: (1) `src/convex/seed.ts:39` — literał `segments`, zasila tabelę; (2) `src/lib/data/voyage-segments.ts:9` — renderuje landing i checkout; (3) tabela Convex `voyageSegments` — prawda w runtime; (4) `dashboard/+page.svelte:212` — tablica `ports` z datami granicznymi etapów w innym formacie (`'4.10'`, `'11.10'`). **Na dziś wszystkie zgodne** — i to jest cała trudność: żaden test ani przegląd tego nie złapie, bo nic nie jest zepsute. Rozjedzie się w dniu, w którym Michał przesunie termin w Convexie: landing pokaże nową datę, panel żeglarza starą, bez żadnego sygnału. Precedens, że to realne: cena s3 zmieniana 07-19 wymagała edycji w **trzech** statycznych źródłach + ręcznego patcha na prod Convex. Ta sama rodzina: `seed.ts:14 BERTH_IDS` powiela `cabins.ts` (te same 10 koi, inny kształt). Kolejność podjęcia: razem z **FEAT-11** (samodzielna edycja przez Michała) i **REFACTOR-2** (`voyageSegments[].id` vs `slug`) — samo usunięcie kopii bez panelu edycji przeniesie tylko problem. ~~Kopia (4) jest najtańsza do zamknięcia od razu: daty da się wyliczyć z segmentów.~~ **✅ punkt (4) — 2026-08-02 (commit `38bf9bef`, na prod, zweryfikowane).** *(Tomek/Claude 07-31 (II), przy INFRA-9)*

  **Punkt (4) — co się okazało przy realizacji.** Daty da się wyliczyć, ale **nie z `voyage-segments.ts`**: pole `dates: '4–11.10.2026'` to napis do wyświetlenia, nie dane. Wycinanie z niego fragmentu związałoby panel z decyzją copywritera i z tłumaczeniem (`'Oct 4–11, 2026'` po angielsku), a separator to półpauza U+2013, nie myślnik. Właściwym źródłem okazały się `startDate`/`endDate` w tabeli Convex — były tam od początku, jako `v.number()`. Oś trasy czyta je przez `listSegments` i formatuje w UTC, bo seed zapisał północ UTC (`seed.ts:44`); odczyt lokalny dałby dzień wcześniejszy dla użytkowników na zachód od Greenwich. Segmenty sortowane jawnie po `startDate` — `listSegments` nie ma `withIndex`, więc kolejność wyniku brała się z kolejności wstawiania przez seed. Powiązane: [[display-string-is-not-a-data-source]], [[calendar-day-vs-instant-in-time]], [[unordered-query-result-is-an-assumption]].

  **Punkt (5) — nazwy portów nie istnieją w bazie (nowe, wyszło przy p.4).** Oś trasy trzyma pięć nazw wpisanych ręcznie (`PORT_NAMES` w `dashboard/+page.svelte`, z komentarzem odsyłającym tutaj). Nie da się ich wyprowadzić z danych: `name` segmentu („Majorka → Gibraltar") to etykieta prezentacyjna i **przeczy** osi trasy — pierwszy port nazywa się tam `Palma de Mallorca`. To ślad po encji, której w projekcie nie ma w ogóle: PORT występuje w trzech rozsypanych postaciach — współrzędne w `route-section.svelte`, nazwy w `PORT_NAMES`, nazwy wklejone w `name` segmentu. Docelowo port jako byt z referencjami z etapu (początkowy/końcowy). Wchodzi z FEAT-11 i REFACTOR-2. Powiązane: [[two-names-for-one-thing-means-a-missing-entity]].

  **Punkt (6) — wzór `N + 1` zakłada trasę liniową (Tomek 08-02).** Oś trasy buduje listę portów z listy etapów: `N` etapów → `N + 1` portów. Prawdziwe tylko dla ścieżki. Rejs okrężny (Mahé → … → Mahé, kandydat na Seszele) to cykl — cztery etapy dają cztery porty. Założenie o topologii jest dziś zaszyte w arytmetyce indeksów i nigdzie nie zapisane. Znika samo, gdy port stanie się encją (punkt 5): dwie referencje wskazujące ten sam wiersz nie wymagają żadnej zmiany w kodzie.
- **REFACTOR-1 — `/book`: jedno źródło prawdy wyboru koi.** Dziś dwa stany o tym samym fakcie: lokalny `selectedSegment`/`selectedBerths` w `book/+page.svelte` + globalny `bookingSelection` (czyta nav), synchronizowane ręcznie w 3 punktach (Plan A, 07-14). Docelowo `/book` czyta/pisze wyłącznie `bookingSelection` (klasa dostaje init z URL), lokalne zmienne znikają. Uwaga na SSR: init do singletona tylko w `if (browser)`. *(decyzja Tomka 07-14 przy feedbacku 07-17)*

- **INFRA-8 — Startowy zestaw zasad dla nowego projektu (kompilacja z vaulta).** Pytanie Tomka 07-26: czy istnieje spisany zestaw reguł, którymi kierowałaby się sesja zakładająca **nowy** projekt — tak, żeby odkrycia robione tu po fakcie (np. `ConvexError` vs `Error` na prodzie, wykluczanie niespójnych stanów na poziomie schematu, „fakt wyliczany nie broni się sam") były znane od pierwszego dnia. Dziś wiedza żyje rozproszona w ~240 artykułach `knowledge-vault/wiki/concepts/`, indeksowana tematycznie, nie w kolejności zakładania projektu. Kierunek: wyłuskać z vaulta pozycje `scope: universal` + `scope: stack` mające charakter **decyzji startowej** (modelowanie danych, granice walidacji, obsługa błędów, auth, i18n, testy, deploy) i złożyć je w jedną procedurę „nowy projekt na tym stacku — zasady od dnia zerowego". Nie jest to przepisywanie artykułów, tylko indeks decyzyjny z linkami. Otwarte: gdzie ma mieszkać (vault `procedures/` vs szablon repo) i jak nie dopuścić do rozjazdu z artykułami źródłowymi. *(Tomek 07-26 #1)*

  **Pierwsza konkretna zasada do tego zestawu — z INFRA-9 (Tomek 07-31 (II), „jak jednoznacznie przekazać następnym sesjom/agentom"):** dwie warstwy obrony przed zaślepkami. **(1) Zapobieganie:** nie renderuj twierdzenia, którego nie umiesz udowodnić — zamiast fałszywego wiersza pusty stan. **(2) Gdy zaślepka musi powstać:** marker `// MOCK(ID):` z **obowiązkowym numerem pozycji backlogu** i opisem *czego brakuje* („brak połączenia z `voyageSegments`"), nie *że jest tymczasowe*. Audyt schodzi wtedy do jednego `rg 'MOCK\('`, a rozjazd marker↔backlog staje się wykrywalny mechanicznie. Wymuszenie: `no-warning-comments` na `warn` (precedens: 5 × `prefer-svelte-reactivity` jako świadomy dług adopcyjny). **Rozstrzygnięcie otwartego pytania „gdzie ma mieszkać" — propozycja:** vault trzyma **uzasadnienie** (jedno źródło, przenośne), repo `AGENTS.md` trzyma **krótki zapis operacyjny z odsyłaczem** (agent czyta na starcie sesji), linter **wymusza bez czytania czegokolwiek**. Warunek: zapis w repo ma być odsyłający, nie kopią artykułu — kopia rozjedzie się z oryginałem, czyli dokładnie ta klasa problemu, którą konwencja zwalcza. Powiązane: [[placeholder-marker-needs-a-backlog-id]].

- **INFRA-10 — Weryfikacja twierdzeń treściowych landingu z Michałem.** Wyszło przy INFRA-9 jako świadoma granica audytu: `pricing-section` (co w cenie / poza ceną), `vessel-section` (specyfikacja jachtu), `faq-section`, `crew-guide.ts` (228 linii treści) to niemal wyłącznie **twierdzenia o świecie** — „Pełne ubezpieczenie jachtu i kaucji", „Jeanneau Sun Odyssey 519", „opłaty portowe ~150–200 EUR/os", odpowiedź o internecie satelitarnym. Nie mają i nie mogą mieć źródła w kodzie, więc nasza weryfikacja zamieniłaby się w zgadywanie o rzeczywistości. Podział, który tu obowiązuje: **twierdzenie o aplikacji → sprawdzamy sami; twierdzenie o świecie → tylko właściciel biznesu.** Zadanie: przekazać Michałowi listę do przejrzenia pod kątem aktualności (ceny opłat portowych, zakres ubezpieczenia, dane jachtu), bez naszej oceny merytorycznej. *(Tomek/Claude 07-31 (II), przy INFRA-9)*

## 🌍 i18n

- **I18N-1 — Uruchomienie EN: teksty z plików danych `.ts` poza wuchale + pusty katalog.** (a) Config wuchale ma tylko adapter `svelte` → pliki danych (`src/lib/data/crew-guide.ts` — całe FAQ/poradnik) nie trafiają do `.po`; wersja EN pokaże je po polsku. Rozwiązanie do wyboru przy podjęciu: drugi adapter `vanilla` dla plików danych ALBO per-locale pliki (`crew-guide.pl.ts` / `crew-guide.en.ts` wybierane po `params.lang`). (b) `en.po` ma niemal wszystkie `msgstr` puste — EN treściowo nie istnieje; uruchomienie = realna praca tłumaczeniowa, nie tylko technika. *(wykryte 07-12 przy copy landing)*

## ❓ Open questions

- **A7d regulamin rejsu** — progi 180/90/42/0 potwierdzone (§3.8); dokument dostarczony 07-07. Czy coś jeszcze otwarte? Zweryfikować.
- **Faktury KSeF:** (a) zagraniczni nabywcy przez KSeF czy poza; (b) termin obowiązku Michała wg obrotu; (c) cert typ 1 od Michała.

## 📥 Feedback — status przetworzenia

> Diff: `ls docs/feedback/` vs ta lista. Każdy plik nieoznaczony ✅ → striażuj do backlogu i dopisz tu. Chroni przed zgubieniem nowego feedbacku.

- ✅ `2026-06-19-uwagi-do-strony.docx` — striażowany 07-10 → „Landing Michała 06-19" (22 poz.)
- ✅ `2026-07-05.md` — striażowany wcześniej → BUG-2/BUG-3/UI-1 (status dopisany 07-13)
- ✅ `2026-07-07.md` — striażowany wcześniej → FEAT-5..8, LEGAL-1/2, UI-2/3 (status dopisany 07-13)
- ✅ `2026-07-10.md` — striażowany wcześniej → BUG-6/avatar, FEAT-5 (status dopisany 07-13)
- ✅ `2026-07-12.md` — striażowany 07-13 → FEAT-5 (rejs Seszele maj 2027, szczegóły + plakat w pozycji FEAT-5)
- ✅ `2026-07-17.md` — striażowany 07-14 → zrealizowany od ręki (1 poz.): nav „Kontynuuj rezerwację →" gdy zalogowany + koje wybrane (commit `d1aba342`)
- ✅ `2026-07-19.md` — striażowany 07-19 → 3 poz. zrobione od ręki w kodzie: (1) cena s3 Madera→Teneryfa 2999→3800 w 3 statycznych źródłach (**prod Convex `voyageSegments.pricePerBerth` s3 do patcha ręcznie** — jak przy f4765d3; sprawdzić też kwoty rat planu płatności s3 w `/admin/automation`); (2) legenda boat-plan „Bezpłatna"→„Niedostępna" (+aria); (3) „zł" przy cenach (pricing + route section) w DM Sans zamiast Playfair — glif „&" u Michała to serifowe/fallbackowe „z". Samodzielna edycja cen przez Michała = FEAT-11 (wzmocniony).
- ✅ `2026-07-17.md` **(dopisek 07-20)** — striażowany 07-24 → LEGAL-3 („zaliczka" → „Wpłata początkowa"); część kodowa + plan prod zrobione 07-24.
- ✅ `2026-07-20.md` — striażowany 07-24 → 2 poz. zrobione od ręki: (1) cron przypomnień o danych załogi pomija zamknięte bookingi (`isBookingClosed` w `_listCrewReminderCandidates`, commit `5957312`); (2) Sales Board: filtr „Zwrócone" + wykluczenie zamkniętych z „Oczekuje wpłaty"/„Opłacone" (commit `906f758`). Oba na prod, zweryfikowane.
- ✅ `Strona jachty.eml` (Michał email 07-23) — striażowany 07-24 → FEAT-15 (sekcja „Program rejsu", blocked na content), FEAT-13 (lightbox), FEAT-5 rozszerzony (strona główna multi-rejs), FEAT-12 (książka + kod −3% + śpiewnik); #5 (QR) = odpowiedź bez kodu (statyczny QR jest trwały).
- ✅ `2026-07-20.md` **(dopisek 07-26, 4 poz. RODO/audit)** — striażowany 07-26 → **UI-10** (kolumna „admin" w `/admin/audit` jako człowiek, nie `subject` Clerka), **LEGAL-4** (podstawa prawna + miejsce + retencja danych wrażliwych, wzór RyanAir, transfery UE-USA, pytanie o podmioty trzecie żądające danych), **LEGAL-5** (audyt odnośników do regulaminu i polityki we wszystkich formularzach i mailach). Punkt o RODO/hostingu częściowo pokryty istniejącymi SEC-1/SEC-2b i LEGAL-1/2 — LEGAL-4 dotyczy treści prawnej, nie kodu.
- ✅ `2026-07-25.md` — striażowany 07-26 → **UI-7** (mobile: CTA rezerwacji siedzi w hamburgerze, ma być widoczne na stałe), **UI-8** (mobile: zawijanie w zakładkach etapów ucina daty), **UI-9** (mobile: po wyborze segmentu przenieść widok do planu jachtu, z wyjątkiem etapu wyprzedanego). Wszystkie trzy ze screenshotami w `docs/assets/`.
- ✅ `2026-07-26.md` — striażowany 07-27 → **INFRA-8** (startowy zestaw zasad dla nowego projektu, wyłuskany z vaulta), **UI-11** (przegląd kontrastu tło↔czcionka w całym projekcie przed przebudową landingu, screenshot `IMG_3269.jpeg`). **Dopisek 07-31 (II):** Tomek wrócił do punktu 1 z konkretem („jak nie produkować zaślepek i jak przekazać zasadę następnym sesjom") — INFRA-8 dostał pierwszą ostrą podpozycję (konwencja `MOCK(ID)` + podział vault/repo/linter) zamiast kolejnej ogólności.
- ✅ `2026-08-10.md` — striażowany 08-13 → **LEGAL-8** (rozbieżność progów zwrotów vs opis Michała, sprawdzone na żywo przeciw prod), **LEGAL-9** (checkbox akceptacji regulamin+umowa uczestnika), **FEAT-17** (automatyzacja zgłoszenia załogi, booking-manager.com — otwarte pytanie o API), **UI-17** (przepisanie języka na składkowy: cena→składka, wyprzedane→brak miejsc, boutique→private, nowy slogan hero). Dopiski do **LEGAL-6** (regulamin v2 przesłany + klauzula zmiany trasy) i **LEGAL-7** (odpowiedź Michała: bez nazwy firmy na stronie, tylko kontakt; forma prawna „prywatny rejs niekomercyjny składkowy" — kandydat na ADR, nienapisany). Nowe dokumenty źródłowe w `docs/assets/` (regulamin v2 `.doc`, umowa uczestnika `.odt`) — nieprzeniesione do `docs/business-decisions/`, czekają na potwierdzenie wersji finalnej.
- ✅ `2026-08-12.md` — striażowany 08-13 → **BUG-10** (panel klienta: rata po terminie pokazuje „Oczekuje" zamiast „Po terminie"/„Zaległe", `dashboard/+page.svelte:104-119` czyta zapisany status zamiast derywować z `dueAt`). Znaleziony przez `git status`, nie zgłoszony ustnie przez Tomka w tej sesji — dopisać do rutyny: sprawdzać `docs/feedback/` diff nie tylko przy `close session`.
- ℹ️ **Diff katalog-vs-lista 2026-08-13 (II): zgodny.** `ls docs/feedback/` = 12 pozycji (10 `.md` + `.docx` + `.eml`), bez zmian od 08-13 rano, wszystkie odhaczone. **Uwaga kierunkowa Tomka na następną sesję:** feedback `2026-08-10.md` jest striażowany, ale jego pozycje są w większości **niezrealizowane** — następna sesja zaczyna od nich (**UI-17**, **LEGAL-9**, **LEGAL-6 p.1-2**; **FEAT-17** czeka na odpowiedź o API booking-manager). Striaż ≠ realizacja.
- ℹ️ **Diff katalog-vs-lista 2026-08-08: zgodny.** `ls docs/feedback/` bez zmian od 07-31 (10 pozycji + `.docx` i `.eml` jako źródła), wszystkie odhaczone. Żadnego nowego feedbacku do striażowania.
- ℹ️ **Diff katalog-vs-lista 2026-08-06: zgodny.** `ls docs/feedback/` bez zmian od 07-31 (10 pozycji + `.docx` i `.eml` jako źródła), wszystkie odhaczone. Żadnego nowego feedbacku do striażowania.
- ℹ️ **Diff katalog-vs-lista 2026-08-05: zgodny.** `ls docs/feedback/` bez zmian od 07-31 (10 pozycji + `.docx` i `.eml` jako źródła), wszystkie odhaczone. Żadnego nowego feedbacku do striażowania.
- ℹ️ **Diff katalog-vs-lista 2026-08-02: zgodny.** `ls docs/feedback/` bez zmian od 07-31, wszystkie pozycje odhaczone. Żadnego nowego feedbacku do striażowania.
- ℹ️ **Diff katalog-vs-lista 2026-07-31: zgodny.** `ls docs/feedback/` = 10 pozycji, wszystkie odhaczone. Żadnego nowego feedbacku do striażowania.
- ℹ️ `2026_07_07_SA_regulamin_rejsu.doc` — nie feedback, dokument źródłowy (regulamin; progi §3.8 wykorzystane przy ADR-002). **Przeniesiony 07-30 do `docs/business-decisions/`** — katalog feedbacku jest z natury przemijający (striażowane → odhaczone → zapomniane), a to źródło prawne żywej reguły biznesowej. Publikacja: **LEGAL-6**.

## ✅ Rozwiązane niedawno (żeby nie wracały)

- ~~BUG-9 — spóźniony webhook płatności nadpisuje `paymentStatus: 'refunded'`~~ — 08-13 (`d0f841cf`, na prod, **niezweryfikowane behawioralnie**). Guard w `applyStripePayment` + `if (!result) break` w webhooku. Decyzja biznesowa → ADR-022. Dwa ogony: **FEAT-18** (powiadomienie Michała — brakująca encja) i smoke test odłożony do FEAT-18.

- ~~REFACTOR-5 — `markOverduePayments` kłamie u źródła~~ — 08-06 (`184fdf01`, na prod + migracja na dev i prod). Naprawa wylądowała w `processStripeRefund`, nie w cronie; `continue` w pętli okazał się przeciekiem, nie naprawą. Reguła biznesowa → ADR-020. Obrona u odbiorców zostaje świadomie. **Follow-up 08-08:** reguła wyekstrahowana do `_lib/refundCancellation.ts` + 6 testów jednostkowych (`29926c93`, na prod); przy tej okazji poprawka zachowania — rata `processing` przestała być anulowana (ADR-020 uzupełnione), co odsłoniło nienaprawiony jeszcze **BUG-9**.
- ~~UI-14 nieaktywne zakładki panelu żeglarza wyglądają na wyłączone~~ — 08-05 (`83cdb043`, na prod). 3.46 → 7.97; przy okazji powstała skala tokenów kontrastu, z której korzysta reszta projektu.
- ~~UI-11 — landing i panel żeglarza~~ (część pozycji; całość otwarta) — 08-05, cztery commity. Skala tokenów + 26 podmian + trzy naprawy hover + gradienty hero; sufit na zdjęciu wydzielony jako UI-16, powtórzony trójstan jako REFACTOR-7.
- ~~REFACTOR-6 punkt (4) — daty osi trasy w panelu żeglarza jako druga kopia~~ — 08-02 (`38bf9bef`, na prod, zweryfikowane). Ręczna tablica pięciu dat zastąpiona `$derived` czytającym `startDate`/`endDate` z Convexa; formatowanie w UTC (seed zapisuje północ UTC); segmenty sortowane jawnie po `startDate`. Nazwy portów zostały wpisane ręcznie z komentarzem — nie ma ich w bazie (REFACTOR-6 p.5).
- ~~INFRA-9 audyt zaślepek i danych makietowych~~ — 07-31 (II). Trzy sygnatury, dwie wykrywalne wzorcem, trzecia (obietnica w akapicie) tylko lekturą. Naprawione w tej samej sesji: przycisk „Kontakt z organizatorem" `disabled` bezwarunkowo → `<a mailto:>`; zdanie „Możesz je aktualizować do 30 dni przed rejsem" usunięte, bo `upsertBookingParticipant` nie ma żadnego guarda czasowego. Reszta rozpisana: REFACTOR-6, UI-15, INFRA-10, LEGAL-7, SEC-5.
- ~~UI-7 CTA rezerwacji ukryte w hamburgerze na mobile~~ — 07-27 (`30aa674d`, na prod). Kontener `.mobile-actions` (CTA + hamburger), duplikat z rozwijanego menu usunięty, ` ` przed strzałką przeciw sierocie typograficznej.
- ~~UI-8 pasek statystyk hero ucinał czwartą kolumnę na mobile~~ — 07-27 (`24716b59`, na prod). `min-width: 0` + `flex: 1` + `min(22px, 5vw)` + ukryta kolumna „etapy"; opis pozycji w backlogu był błędny (wskazywał zakładki etapów).
- ~~BUG-8 monity o zaległe raty do zwróconych rezerwacji~~ — 07-26 (`93631829`, na prod). Trzy maile wyszły z produkcji rano; oba crony płatnicze dostały guard `isBookingClosed`.
- ~~BUG-7 walidacja formy edycji uczestnika (zod, klient + serwer)~~ — kod `f37903b0`, smoke test warstwy serwerowej zaliczony 07-26. Sekcja bugów pusta pierwszy raz.
- ~~REFACTOR-3 reguła holdu w wielu kopiach~~ — 07-26 (`0d832bcf` + `aea3c53e`, na prod). Jedno źródło (`_lib/berthFree.ts`) + 7 testów; helper przyjął ostrożniejszą regułę guarda sprzedaży, nie odwrotnie.
- ~~FEAT-8 badge dostępności (Wyprzedane / Wolne N miejsc)~~ — 07-25 (`d8f9b246`, na prod). Zaszyty „Ostatnie miejsca" na 4. karcie cennika usunięty — kłamał (etap z 9 wolnymi kojami).
- ~~FEAT-13 lightbox galerii jachtu~~ — 07-24 (`2b25789d`: natywny `<dialog>` w `vessel-section.svelte`, prev/next wrap-around, Esc/×/strzałki/backdrop). Znane ograniczenie: źródła low-res (max 698×548) → sufit `min(92vw,1000px)`; pełne zdjęcia z FEAT-5e/FEAT-15.
- ~~Nav „Rezerwuj" statyczny mimo zalogowania + wyboru koi (feedback 07-17)~~ — 07-14 (`d1aba342`: `reserveLabel` w site-nav + sync `/book` → `bookingSelection`; na main, prod czeka na zbiorczy push).
- ~~Panel żeglarza pokazywał zły/wygasły booking~~ — 07-05 (`bookingByUser` lista + filtr `confirmed`).
- ~~Panel żeglarza: tylko jedna koja mimo wielu~~ — 07-05 (selektor rejsu).
- ~~PDF potwierdzenia font poza bundlem~~ — 07-07 (`?url` + `read()`).
- ~~Refund webhook race → podwójny refund~~ — 07-07 (match po `refundRowId`).
- ~~Refund policy retroaktywny~~ — 07-09 (snapshot przy zakupie, ADR-002) — **czeka deploy DEP-1**.
- ~~Audit log brak UI~~ — 07-10 (`/admin/audit` MVP) — **czeka deploy DEP-1**.
