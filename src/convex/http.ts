import { httpRouter } from 'convex/server'

const http = httpRouter()

// Register HTTP actions here
// Example:
// http.route({
//   path: '/webhook/stripe',
//   method: 'POST',
//   handler: httpAction(async (ctx, req) => { ... }),
// })

export default http
